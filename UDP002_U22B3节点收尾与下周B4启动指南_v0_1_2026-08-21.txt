UDP002 U22B3 节点收尾与下周 B4 启动指南
========================================

版本：v0.1
日期：2026-08-21

一、当前结论
------------

B3 已通过公司真实数据验收：

B3R N-M7-A7-R271-O40-V40-E40-B1-G0-X0 C-0

S0-S5 正式冻结用于 MVP Golden Run。不要修改已安装的 S4/S5 Module、
已验收的 Run Configuration 或 07_OUTPUT 中的 B3 Run。


二、清理 04_TEMP
----------------

1. 下载：

NODE_CLOSE_CLEANUP_20260821_v0_1.cmd.txt

2. 在下载目录把尾缀从 .cmd.txt 改为 .cmd。

3. 在 C-CODE 目录使用 CMD 执行：

call "%USERPROFILE%\Downloads\NODE_CLOSE_CLEANUP_20260821_v0_1.cmd" U-UDP002

该程序只把 04_TEMP 顶层名称含 U22B1、U22B2、U22B3 的临时文件或目录
移动到：

U-UDP002\04_TEMP\A-Archive\U22B3_CLOSE_20260821

不会删除文件，也不会修改 01_PROGRAMS、02_CONFIG、03_CASES 或 07_OUTPUT。

成功时只需确认：

U22B3CL N-M数字-A1-K3-X0 C-0


三、下周正式入口：B4 / S6 Candidate Synthesis
------------------------------------------------

下周不要直接猜测 Threshold。第一步是 B4A，只读分析已经验收的 S5
ROBUST_RESIDUAL_EVIDENCE Artifact，先输出：

- 有效 score 数量；
- q50、q90、q95、q99 和最大值；
- score 超过 2、3、4、5 的行数；
- 连续超阈值区间数量和最长持续帧数；
- 最高分 FrameCount；
- 候选密度。

B4A 不修改 Artifact、不生成正式 Event，也不调整 S4/S5。

获得真实分布后依次进行：

B4A  Evidence只读画像
-> B4B S6 Algorithm Card与Policy
-> B4C S6 Module实现和本机完整联动测试
-> B4D 公司真实数据验收
-> C1   S7 Review Package交给U003完成Golden Run


四、S6固定设计约束
------------------

- 输出是调查候选，不是Root Cause。
- Threshold、Persistence、Merge、Spacing和Top-K必须明确记录。
- Full Log最多输出40个可行动候选。
- 同一异常过程不能连续生成大量重复Case。
- 每个候选至少保留Start、Peak、End FrameCount、Peak Score、Reason、
  Importance、Module Identity和Source Artifact。
- 默认目标间隔参考5秒，但最终参数必须经过B4A/B4B确认。
- 正式候选生成只使用有效、可追溯的S5 Evidence行。


五、周一第一句话
----------------

直接回到本Project输入：

B4-START

届时从B4A Evidence Profiler开始，不重新Probe B1-B3。
