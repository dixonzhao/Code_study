UDP002 U21E GATE TYPEERROR CORRECTION

Task ID: U21E-GATE-FIX-01
Instruction Version: 0.1
Date: 2026-08-18
Owner: Zhao Zhili
Status: APPROVED FOR ONE GATE-LOCAL CORRECTION


1. CONFIRMED ROOT CAUSE

Diagnostic return:

  U21EGD1 E-TypeError
  F-u002_u21e_acceptance_gate_diagnostic_v0_1.py.txt
  L-52 FN-request C-0
  MEMO-the JSON object must be str, bytes or bytearry, not WindowsPath

The Gate-local `request` function passes a `pathlib.WindowsPath` object directly
to `json.loads()`. `json.loads()` parses JSON text; it does not open a filesystem
path. No Binder, Module Pack, Fixture or contract defect has been demonstrated.


2. INPUTS TO COPILOT

Provide exactly:

1. this correction instruction;
2. original formal Gate source:
   u002_u21e_acceptance_gate_v0_1.py.txt;
3. diagnostic Gate source and retained traceback only as reference;
4. the exact command/arguments used for the failed formal Gate.

Do not edit the U21E Binder, qualified plan, Module Packs, U001 package,
fixtures or frozen contracts.


3. GENERATE ONE VERSIONED FORMAL GATE

Generate the complete downloadable file:

  u002_u21e_acceptance_gate_v0_2.py.txt

Base it on the original formal Gate v0.1, not on a partially modified file.

Permitted production-code correction only:

- inside function `request`, convert the supplied path-like value to `Path`;
- read that file as JSON text with `encoding="utf-8-sig"`;
- pass the resulting text to `json.loads`, or equivalently use `json.load` on
  the opened text stream;
- preserve all existing schema checks, acceptance assertions, ordering,
  output naming, exit codes and capsule semantics.

Reference form (adapt names to the existing function without redesign):

  from pathlib import Path

  def request(json_path):
      path = Path(json_path)
      return json.loads(path.read_text(encoding="utf-8-sig"))

Do not convert the Path with `str(path)` and then pass that filename string to
`json.loads`; that would parse the filename as JSON rather than read the file.


4. REQUIRED REGRESSION SELF-TEST

Extend the Gate's existing self-test with one focused regression case:

- create a temporary JSON file using a `Path` object;
- include a UTF-8 BOM-compatible read case;
- call `request(temp_path)`;
- assert that the parsed object exactly equals the written object;
- confirm the temporary path and content are not modified.

Existing self-tests must remain and pass. Do not weaken any assertion to obtain
a pass.


5. EXECUTION

1. Run the v0.2 Gate self-test.
2. Run v0.2 with the exact same Binder/plan/Module inputs as the failed formal
   Gate, using a fresh non-existing output directory such as:

     U21E_GATE_V02_RUN_01

3. Do not reuse or overwrite the failed Gate or diagnostic output directories.
4. If another failure occurs, do not modify Binder or validator logic. Preserve
   the full traceback/evidence and stop.


6. RETURN

If self-test and formal Gate both pass, return only the Gate's normal compact
acceptance capsule, prefixed with:

  U21EG2 <normal Gate capsule> C-0

If it fails, return only:

  U21EG2D E-<ExceptionType> F-<SourceBasename> L-<LineNumber> FN-<FunctionName> C-7
  MEMO-<maximum 120 characters, exact exception or failed assertion>


7. ACCEPTANCE BOUNDARY

A v0.2 Gate pass accepts the exact Binder identity under test. It does not
authorize unrelated refactoring. Promotion remains a separate byte-identity and
relocated-self-test step.

END
