UDP001 — EXECUTION GATE 1
U001 FOUNDATION CORE
Date: 2026-07-24

======================================================================
1. GATE DECISION
======================================================================

Gate 0 result:

PROCEED_WITH_CONDITIONS

Approved first implementation scope:

- M02 CSV/header detector;
- M03 DBC parser and decode primitives;
- M04 signal inventory and selection;
- M01 configuration/schema dataclasses;
- synthetic self-tests;
- read-only real-source inventory probe.

This gate creates a pure-logic foundation only.

Do not create GUI.
Do not decode the full CAN log.
Do not sort records.
Do not forward fill.
Do not fill blanks with zero.
Do not implement FrameCount assignment.
Do not modify P001/P002/P003.
Do not inspect P004.

======================================================================
2. FILES TO GIVE COMPANY COPILOT IN A NEW CHAT
======================================================================

Legacy source:

1. can_header_catalog_gui.py
2. matlab_can_time_order_decoder_gui.py
3. can_analysis_csv_cleaner_gui.py

Company-side Gate 0 output:

4. UDP001_legacy_reuse_assessment_v0_1.txt

Control documents downloaded from GitHub:

5. UDP001_G1_00_START_HERE_2026-07-24.txt
6. UDP001_G1_01_UNIFIED_DATA_CONTRACT_v0_1_2026-07-24.txt
7. UDP001_G1_02_FOUNDATION_CORE_IMPLEMENTATION_SPEC_v0_1_2026-07-24.txt
8. UDP001_G1_03_FOUNDATION_CORE_ACCEPTANCE_PROTOCOL_v0_1_2026-07-24.txt

Do not attach company data or DBC during code generation.

======================================================================
3. EXACT MESSAGE TO COMPANY COPILOT
======================================================================

请完整读取本对话附带的8个文件。

严格执行
UDP001_G1_02_FOUNDATION_CORE_IMPLEMENTATION_SPEC_v0_1_2026-07-24.txt。

Gate 0已经批准为PROCEED_WITH_CONDITIONS。本任务只实现U001纯逻辑基础核心，
不得合并GUI，不得修改P001/P002/P003，不得生成完整U001，不得加入排序、
forward fill、补0、FrameCount assignment、aggregation、hold或分析功能。

请复用Gate 0报告允许复用的源码逻辑，但必须按冻结的Data Contract重新定义
接口。没有证据时不得自行发明公司信号规则。

请创建并提供一个可下载的纯文本代码文件：

u001_foundation_core_v0_1.py.txt

文件内容必须是完整Python源码。不要只在聊天中粘贴代码，不要返回伪代码，
不要拆成多个程序文件。

生成后只在聊天中回答：

UDP001-G1-CODE-GENERATED

如无法完整生成文件，回答：

UDP001-G1-CODE-NOT-GENERATED

并用一行说明原因。

======================================================================
4. AFTER DOWNLOAD
======================================================================

In the company work folder:

1. Keep the downloaded `.py.txt` as the transfer copy.
2. Create a working copy named:

   u001_foundation_core_v0_1.py

3. Run:

   python u001_foundation_core_v0_1.py --self-test

4. Only if self-test passes, run the read-only probe:

   python u001_foundation_core_v0_1.py --probe ^
     --source "RAW_MATLAB_CAN_CSV_PATH" ^
     --dbc-ch1 "CH1_DBC_PATH" ^
     --dbc-ch2 "CH2_DBC_PATH" ^
     --out "OUTPUT_FOLDER"

In VS Code PowerShell, the command may be entered on one line without `^`.

The probe must read only the source header/sample needed for format detection.
It must not scan or decode the full one-hour log.

======================================================================
5. EXPECTED OUTPUT
======================================================================

Self-test console:

UDP001-G1-SELFTEST-PASS-16

Probe output folder:

- udp001_g1_signal_catalog.csv
- udp001_g1_probe_summary.json
- udp001_g1_return_capsule.txt

Return to ChatGPT only the content of:

udp001_g1_return_capsule.txt

Do not manually transcribe the catalog or JSON.

======================================================================
6. STOP CONDITIONS
======================================================================

Stop and return the failure capsule when:

- the generated file is incomplete;
- Python syntax fails;
- any self-test fails;
- importing the module opens a GUI;
- the source file is fully scanned during the probe;
- DBC parsing fails;
- no source byte groups are found;
- no catalog match is produced;
- any legacy source file is modified.

Do not ask Copilot to patch failures freely. Return the capsule first so the
architecture side can classify the defect and issue one controlled correction.
