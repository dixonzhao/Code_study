UDP002 G2F2 — EXECUTOR v0.4 INSTALL AND SYNTHETIC VERTICAL RERUN

Version: 0.1
Date: 2026-08-19
Status: EXECUTE EXACTLY / FAIL CLOSED


1. PURPOSE

U21F Executor v0.4 has passed its expanded internal self-test:

  U21F2B4 N-T46-R4-M12-A9-F8-I8-E8-Q6-X0 C-0

This proves the candidate's internal regression surface, but does not yet:

- install v0.4 as a governed runtime;
- prove candidate and installed bytes are identical;
- prove the frozen dependencies remain unchanged;
- prove the full G2 synthetic vertical chain runs through v0.4.

G2F2 performs those remaining operations without overwriting v0.3.


2. REQUIRED WORKING SET

All transferred .txt files remain directly in:

  U-UDP002\04_TEMP

Required before starting:

  u002_controlled_stage_executor_v0_4.py.txt
  u002_g2_synthetic_vertical_run_v0_1.py.txt
  u002_g2f2_executor_v04_installer_v0_1.py.txt
  u002_g2_v04_vertical_run_v0_2.py.txt

The first two already exist from G2F1/G2. Download only the final two files
from the current public transfer packet. Do not move required validators or
contracts into A-Archive during this run.


3. DO NOT DO

- Do not ask Copilot to regenerate or edit either delivered program.
- Do not rename transferred source files before execution.
- Do not overwrite or delete u002_controlled_stage_executor_v0_3.py.
- Do not edit U21D Plan, U21E Preflight, G2 v0.1 base or retained evidence.
- Do not reuse the old G2 output directory.
- On C-7, stop. Do not repair evidence or retry with modified inputs.


4. EXECUTION — WINDOWS CMD FROM C-CODE ROOT

Step 1 — local installer self-test

  python U-UDP002\04_TEMP\u002_g2f2_executor_v04_installer_v0_1.py.txt --self-test

Expected:

  G2F2ST N-T28-P7-J7-I7-X0 C-0

Normal C-0 self-test does not need to be returned.


Step 2 — governed v0.4 installation

  python U-UDP002\04_TEMP\u002_g2f2_executor_v04_installer_v0_1.py.txt --install --workspace-root U-UDP002

Expected:

  G2F2 N-A4-D2-S2-H2-R1-I8-X0 C-0

It installs a new file:

  U-UDP002\01_PROGRAMS\u002_controlled_stage_executor_v0_4.py

It does not replace v0.3.


Step 3 — v0.4 G2 wrapper self-test

  python U-UDP002\04_TEMP\u002_g2_v04_vertical_run_v0_2.py.txt --self-test --workspace-root U-UDP002

Expected:

  G2V2ST N-T16-B1-O3-I4-X0 C-0

Normal C-0 self-test does not need to be returned.


Step 4 — fresh v0.4 synthetic vertical run

Confirm neither of these exists first:

  U-UDP002\05_TESTS\02_ACCEPTANCE\U21G_G2_VERTICAL_V02
  U-UDP002\05_TESTS\02_ACCEPTANCE\U21G_G2_VERTICAL_V02.staging

Then run:

  python U-UDP002\04_TEMP\u002_g2_v04_vertical_run_v0_2.py.txt --run --workspace-root U-UDP002 --out-dir U-UDP002\05_TESTS\02_ACCEPTANCE\U21G_G2_VERTICAL_V02

Expected:

  G2V2 N-M3-B1-E1-R1-S8-A3-I17-X0 C-0


5. RETURN — TWO LINES MAXIMUM

Return only:

  G2F2 <exact installer capsule>
  G2V2 <exact vertical-run capsule>

If any command gives C-7, return only that exact C-7 line and stop.


6. ACCEPTANCE MEANING

G2F2 C-0 means:

- candidate and installed v0.4 both passed dependency preflight;
- candidate and installed v0.4 both passed the 46-check self-test;
- installed source is byte-identical to the candidate;
- eight frozen dependency inputs remained unchanged;
- v0.3 remains as the rollback/reference baseline.

G2V2 C-0 means:

- the full synthetic control chain used installed Executor v0.4;
- Registry, Activation, Plan, Preflight, Executor and evidence validation
  completed as one governed vertical run;
- the ExecutionOrder contract mismatch is closed in integration, not merely
  in an isolated self-test.

Only after both capsules are C-0 may G2F2/G2V2 be promoted and U21G closeout
continue.

END OF INSTRUCTION
