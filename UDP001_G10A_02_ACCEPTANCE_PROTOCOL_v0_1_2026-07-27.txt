UDP001 G10A - AUTO DISCOVERY PROFILE BUILDER
ACCEPTANCE AND RETURN PROTOCOL v0.1
Date: 2026-07-27

======================================================================
1. GENERATION RULE
======================================================================

Company Copilot shall generate one downloadable file:

u001_auto_discovery_profile_builder_v0_1.py

Save it as:

C-CODE\UDP001\04_TEMP\u001_auto_discovery_profile_builder_v0_1.py

Do not modify existing U001 files during G10A.

======================================================================
2. SELF-TEST
======================================================================

Run from C-CODE using CMD:

python .\UDP001\04_TEMP\u001_auto_discovery_profile_builder_v0_1.py --self-test

Required exact final line:

UDP001-G10A-SELFTEST-PASS-18

The 18 deterministic tests must cover:

T01 import safety;
T02 required catalog columns;
T03 MATCHED and complete becomes eligible;
T04 SOURCE_ONLY is excluded with reason;
T05 DBC_ONLY is excluded with reason;
T06 incomplete source group is excluded with reason;
T07 CanonicalName normalization;
T08 CanonicalName collision blocks;
T09 missing FrameCount blocks;
T10 duplicate FrameCount blocks;
T11 FrameCount override is exact;
T12 invalid proposals receive documented fallbacks;
T13 G2 schema and Selected/Required values;
T14 G4 NONE-validity provisional policy;
T15 G5 aggregation proposals;
T16 G6 NONE-hold policy;
T17 stable 32-signal batch mapping and final partial batch;
T18 output hashes, manifest counts, and return capsule agree.

Stop if self-test fails.  Do not repair existing Gate programs to make this
new program pass.

======================================================================
3. ACTUAL CATALOG RUN
======================================================================

Use the accepted Gate 1 u001_signal_catalog.csv generated from
TC001_20260422.  Company Copilot may resolve the exact existing path; do not
move or rename the catalog merely to satisfy the command.

Create a new output folder under:

C-CODE\UDP001\03_CASES\TC001_20260422\01_CONFIG\AUTO_DISCOVERY_ALL_V0_1

Run from C-CODE using CMD on one line:

python .\UDP001\04_TEMP\u001_auto_discovery_profile_builder_v0_1.py --build --catalog "ACTUAL_PATH_TO_u001_signal_catalog.csv" --out ".\UDP001\03_CASES\TC001_20260422\01_CONFIG\AUTO_DISCOVERY_ALL_V0_1" --profile-id "AUTO_DISCOVERY_ALL_V0_1" --frame-key-channel 2 --frame-key-can-id 0x620 --frame-key-signal FrameCount --batch-size 32

If the output folder already contains files, stop and report it.  Do not add
--overwrite unless Codex explicitly approves the replacement.

======================================================================
4. ACCEPTANCE CHECK
======================================================================

Confirm:

- exactly eight required files exist;
- no input file changed;
- exactly one eligible FrameCount row is Required=1;
- every other eligible signal is Selected=1 and Required=0;
- every excluded catalog row has ExclusionReason;
- every discovery candidate has exactly one batch-map row;
- batch IDs are consecutive;
- no batch exceeds 32 candidates;
- all G6 rows use HoldPolicy=NONE;
- all G4 rows use ValidityRule=NONE;
- manifest hashes and row counts match physical files;
- capsule C-0.

Do not run the complete G8A pipeline with the all-signal controls yet.  G10B
must add bounded-width discovery processing before full Raw CAN execution.

======================================================================
5. RETURN TO CODEX
======================================================================

Return only:

1. exact self-test final line;
2. exact contents of udp001_g10a_return_capsule.txt;
3. up to three short memos only when an acceptance item fails.

Expected compact form:

G10AST-UDP001-G10A-SELFTEST-PASS-18
G10AR1-UDP001G10A1 N-C#-E#-X#-V#-B#-F#-H0 C-0

After C-0, do not promote the program automatically.  Wait for Codex to
approve G10A and issue G10B.

