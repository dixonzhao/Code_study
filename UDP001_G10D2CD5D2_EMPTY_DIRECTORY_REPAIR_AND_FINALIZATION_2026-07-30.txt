UDP001 G10D2C-D5D2 — EMPTY DIRECTORY REPAIR AND FINALIZATION
Date: 2026-07-30
Status: ROOT CAUSE CONFIRMED; ONE NARROW REPAIR AUTHORIZED

Accepted location evidence:

G10D2CD5D1 N-R1-T1-O1-P1-U1-C0-M1-G1-A2-D4-X0 C-0

Root cause:

- copied release, package, portable UDP001 root, incomplete marker and exact Gate all exist;
- C0 means UDP001\03_CASES directory itself is missing;
- the D3 copier copied files and parent folders but did not preserve an empty directory;
- the Gate uses the same CASES_NOT_EMPTY code for a missing directory and a non-empty directory;
- D4 counted zero files but did not distinguish an absent tree from an empty tree.

The release payload is not contaminated. One structural repair is authorized:
create the required empty UDP001\03_CASES directory.

Download u001_g10d2cd5d2_empty_directory_repair_finalizer_v0_1.py.txt, remove
only the final .txt and place it in UDP001\04_TEMP. From C-CODE Windows CMD:

python .\UDP001\04_TEMP\u001_g10d2cd5d2_empty_directory_repair_finalizer_v0_1.py --udp001-root .\UDP001 --out .\UDP001\04_TEMP\G10D2C_RELEASE_01

The program:

1. validates the exact release and Gate locations;
2. creates only the missing 03_CASES directory, or accepts it if already empty;
3. refuses to continue if 03_CASES contains anything;
4. runs the original independent Gate exactly once;
5. rechecks that 03_CASES still exists and remains empty;
6. removes .G10D2C_INCOMPLETE only after full success.

Expected success:

G10D2CD5D2 N-R1-C1-E1-G1-P1-H12-Z1-M1-X0 C-0

Return the full G10D2CD5D2 line. If M1/M2/M3 are printed, return them too.
After C-0, launch the GUI using the command printed in the previous D5 guide.

Future builder correction (not part of this recovery):

- portable package assembly must explicitly create required empty skeleton folders;
- the independent Gate must report CASES_DIR_MISSING separately from CASES_NOT_EMPTY;
- diagnostics must distinguish a missing tree from an empty tree.

