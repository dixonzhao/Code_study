/opt/homebrew/Library/Homebrew/cmd/shellenv.sh: line 18: /bin/ps: Operation not permitted
UDP002 U21F R0 CLEAN RESET

Task ID: U21F-R0
Version: 0.1
Date: 2026-08-19
Owner: Zhao Zhili
Status: APPROVED FOR INVENTORY AND ARCHIVE MOVE ONLY


1. PURPOSE

Create a clean human-auditable U21F starting point without deleting evidence or
touching the accepted U2-0A/U21A-U21E baseline.

This task archives U21F attempt 1. It does not generate U21F v0.2.


2. FRESH COPILOT CONTEXT

Use the new Copilot chat. Provide only this instruction.

Generate exactly one file without Markdown fences:

  u002_u21f_reset_archiver_v0_1.py.txt

Do not generate CMD, BAT, PowerShell, Office files or a second Python file.


3. PROGRAM BOUNDARY

Python standard library only. Implement exactly:

  --self-test

  --scan
  --workspace-root PATH
  --archive-dir PATH

  --apply
  --workspace-root PATH
  --archive-dir PATH

Exactly one action per invocation.

The company workspace root supplied at runtime is U-UDP002, not C-CODE.


4. STRICT U21F ATTEMPT-1 OWNERSHIP RULES

Inspect only these two controlled locations:

  <workspace-root>/04_TEMP
  <workspace-root>/05_TESTS/02_ACCEPTANCE

Select only:

A. 04_TEMP top-level files whose basename begins with one of:

  u002_module_runtime_protocol_bundle_v0_1
  u002_module_runtime_protocol_validator_v0_1
  u002_controlled_stage_executor_v0_1
  u002_controlled_stage_executor_diagonstic_v0_1
  u002_controlled_stage_executor_diagnostic_v0_2
  u002_u21f_acceptance_gate_v0_1
  u002_u21f_gate_denial_diagnostic_v0_1
  u002_u21f_gate_index2_probe_v0_1
  U21F

B. 04_TEMP directories whose basename begins with:

  U21F

C. 05_TESTS/02_ACCEPTANCE directories whose basename begins with:

  U21F

Do not use broad substring matching outside these rules.

Explicitly exclude the running reset program:

  u002_u21f_reset_archiver_v0_1.py.txt


5. MANDATORY PRESERVATION DENYLIST

Never select or move any path whose basename begins with or equals:

  u002_core_contract_
  u002_engine_skeleton_
  u002_module_registry_
  u002_module_activation_controller_
  u002_qualified_module_plan_resolver_
  u002_run_preflight_data_binder_
  u002_u21a_
  u002_u21b_
  u002_u21c_
  u002_u21d_
  u002_u21e_

Never inspect or change 01_PROGRAMS, 02_CONTROLS, 03_CASES, U20A fixtures,
U001 packages or A-Archive content.

If an allow rule and deny rule conflict, deny wins and the program stops C-7.


6. SCAN MODE

Scan is read-only. It must:

- reject a workspace root that lacks 04_TEMP and 05_TESTS;
- reject an archive-dir outside <workspace-root>/A-Archive;
- require archive-dir to be absent;
- enumerate exact selected files/directories in deterministic relative-path
  order;
- stream SHA-256 for every selected file;
- recursively inventory selected directories without following symlinks;
- reject symlinks, path traversal and duplicate ownership;
- calculate file count, directory-root count and total bytes;
- retain no new file in the workspace;
- print only the compact scan capsule.

Success:

  U21FR0S N-F<files>-D<directory_roots>-B<bytes>-E1-X0 C-0

E1 means the reset program was explicitly excluded.


7. APPLY MODE

Apply must independently repeat the complete scan and then:

1. atomically create archive-dir;
2. create subdirectories preserving original controlled-root identity:

     04_TEMP
     05_TESTS_02_ACCEPTANCE

3. move each selected item without overwrite;
4. verify every moved file SHA-256 against its pre-move hash;
5. write reset_manifest.csv with:

     OriginalRelativePath
     ArchivedRelativePath
     ItemKind
     ByteSize
     SHA256
     MoveStatus

6. write deterministic reset_summary.json and README.txt;
7. rescan the two active roots and require zero owned attempt-1 item remains,
   excluding only the running reset program;
8. never delete data as a fallback for failed movement;
9. on partial move, retain a recovery manifest and return C-7.

Success:

  U21FR0A N-F<files>-D<directory_roots>-H<verified_files>-L0-E1-X0 C-0

L0 means zero owned attempt-1 item remains active, excluding the reset program.


8. SELF-TEST

Use private temporary directories and prove exactly twenty-four checks:

- exact allowlist selection;
- denylist precedence;
- reset-program exclusion;
- two-root scope only;
- archive containment;
- absent archive requirement;
- symlink and traversal denial;
- deterministic order;
- streaming hash and byte count;
- dry scan has no writes;
- apply move and hash reconciliation;
- no overwrite;
- partial-failure recovery manifest;
- residual-owned-item detection;
- preservation of unrelated U20A/U21A-U21E fixtures.

Success:

  UDP002-U21F-R0-SELFTEST-PASS-24 C-0

Do not manually return the self-test line when it passes.


9. COMPANY EXECUTION FROM C-CODE ROOT

Download the generated reset program directly to U-UDP002/04_TEMP.

Run self-test:

  python ".\U-UDP002\04_TEMP\u002_u21f_reset_archiver_v0_1.py.txt" --self-test

Run read-only scan:

  python ".\U-UDP002\04_TEMP\u002_u21f_reset_archiver_v0_1.py.txt" --scan --workspace-root ".\U-UDP002" --archive-dir ".\U-UDP002\A-Archive\U21F_ATTEMPT1_20260819"

If scan is C-0, run apply without waiting for another instruction:

  python ".\U-UDP002\04_TEMP\u002_u21f_reset_archiver_v0_1.py.txt" --apply --workspace-root ".\U-UDP002" --archive-dir ".\U-UDP002\A-Archive\U21F_ATTEMPT1_20260819"

Do not manually move or delete additional files after apply.


10. REQUIRED RETURN

Return only the two capsules:

  U21FR0S ...
  U21FR0A ...

Add one short Memo only on C-7. Do not return the manifest rows manually.


11. SUCCESS BOUNDARY

R0 scan/apply C-0 proves the U21F attempt-1 active workspace is clean and all
owned evidence is recoverable under A-Archive. It authorizes U21F v0.2 design,
not implementation, Gate execution, U21G or real data.

END
