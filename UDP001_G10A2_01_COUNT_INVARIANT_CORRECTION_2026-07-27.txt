UDP001 G10A2 - COUNT INVARIANT CORRECTION
Date: 2026-07-27
Status: BLOCKING CORRECTION; G10A1 NOT ACCEPTED

OBSERVED G10A1 CAPSULE

UDP001G10A1 N-C705-E706-X406-V706-B23-F8-H0 C-0

BLOCKING CONTRADICTIONS

The approved G10A definitions are:

- C = physical Gate 1 catalog row count;
- E = technically eligible signal count, including exactly one FrameCount;
- X = excluded catalog row count;
- V = discovery candidate count, excluding FrameCount;
- B = ceil(V / batch_size);
- F = total provisional fallback count;
- H = output hash failure count.

Success invariants:

1. C = E + X
2. E >= 1
3. exactly one eligible FrameCount exists
4. V = E - 1
5. B = ceil(V / batch_size)
6. H = 0

The observed capsule violates invariants 1 and 4:

- E706 is greater than C705;
- E706 + X406 does not equal C705;
- V706 does not equal E706 - 1.

B23 is arithmetically compatible with V706 at batch size 32, but this does not
repair the other contradictions.  C-0 is therefore invalid.

REQUIRED COPILOT INPUTS

Give Copilot:

1. the current generated u001_auto_discovery_profile_builder_v0_1.py;
2. this correction file.

REQUIRED DELIVERABLE

Return one directly downloadable file:

u001_auto_discovery_profile_builder_v0_2.py

Do not modify any other U001 program or existing output folder.

REQUIRED CORRECTION

1. Calculate catalog_row_count from the complete parsed input catalog before
   eligibility filtering.
2. Partition every catalog row into exactly one of ELIGIBLE or EXCLUDED.
3. Calculate eligible_signal_count from eligible rows.
4. Require exactly one eligible configured FrameCount row.
5. Calculate candidate_signal_count from eligible rows excluding FrameCount.
6. Calculate batch_count from candidate_signal_count and batch_size.
7. Use those exact values consistently in inventory, batch map, manifest, and
   return capsule.
8. Add a single validate_count_invariants function.  Run it before writing C-0.
9. Any invariant failure must produce C-7 and must not claim successful output.
10. Do not hard-code the expected real-data counts.

MANIFEST REQUIREMENT

The manifest must expose exact integer fields:

- catalog_row_count
- eligible_signal_count
- excluded_signal_count
- candidate_signal_count
- frame_key_count
- batch_count
- batch_size
- fallback_value_kind_count
- fallback_signal_role_count
- fallback_update_mode_count
- fallback_total_count
- hash_failure_count

SELF-TEST

Retain the original 18 tests and add:

T19 observed-pattern regression: reject C705/E706/X406/V706 as inconsistent;
T20 coherent partition example: C1112/E706/X406/V705/B23 passes at batch 32.

Required exact self-test final line:

UDP001-G10A2-SELFTEST-PASS-20

ACTUAL RERUN

Do not overwrite AUTO_DISCOVERY_ALL_V0_1.  Run v0.2 into:

C-CODE\UDP001\03_CASES\TC001_20260422\01_CONFIG\AUTO_DISCOVERY_ALL_V0_2

Use profile ID:

AUTO_DISCOVERY_ALL_V0_2

Use the same accepted Gate 1 signal catalog, FrameCount identity, and batch
size 32 as G10A1.

RETURN

Return only:

G10A2ST-UDP001-G10A2-SELFTEST-PASS-20
G10A2R1-<exact udp001_g10a_return_capsule.txt line>

Up to three short memos are allowed only on C-7.

