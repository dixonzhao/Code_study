UDP001 G10A - AUTO DISCOVERY PROFILE BUILDER
START HERE
Date: 2026-07-27
Status: IMPLEMENTATION REQUIRED BEFORE U001 FINAL ACCEPTANCE

======================================================================
1. WHY THIS GATE EXISTS
======================================================================

The current U001 smoke path decodes a manually frozen six-signal profile.
That proves the data contract and processing chain, but it cannot support the
project requirement to screen approximately 700 CAN variables automatically.

G10A adds the missing machine-scale signal entry point.

U001 shall automatically select every source-present, DBC-matched, technically
decodable signal for AUTO_DISCOVERY.  This is an availability decision only.
U001 must not claim statistical relevance, causal influence, reference status,
or ground truth.  U002 will later screen the discovery variables automatically.

======================================================================
2. FILES TO GIVE COMPANY COPILOT
======================================================================

Give these three files together in a new Copilot chat:

1. UDP001_G10A_00_START_HERE_AUTO_DISCOVERY_2026-07-27.txt
2. UDP001_G10A_01_AUTO_DISCOVERY_PROFILE_BUILDER_SPEC_v0_1_2026-07-27.txt
3. UDP001_G10A_02_ACCEPTANCE_PROTOCOL_v0_1_2026-07-27.txt

No company data or DBC file is required for code generation.  The program must
contain deterministic synthetic self-tests.

======================================================================
3. REQUIRED COPILOT DELIVERABLE
======================================================================

Copilot must generate and return one directly downloadable Python file:

u001_auto_discovery_profile_builder_v0_1.py

Do not return code only as chat text.  Return the actual .py file.

The program may use only:

- Python standard library;
- pandas.

Do not modify any existing Gate 1-G8 program, GUI, portable host, package
builder, Case source file, DBC file, or current six-signal control profile.

Place the generated program in:

UDP001\04_TEMP\u001_auto_discovery_profile_builder_v0_1.py

It will be promoted to UDP001\01_PROGRAMS only after G10A acceptance.

======================================================================
4. SCOPE BOUNDARY
======================================================================

G10A generates a deterministic discovery inventory, four provisional control
CSVs, and a stable batch map from the existing Gate 1 signal catalog.

G10A does not decode the Raw CAN file and does not modify the current G8A
orchestrator.  Batch-aware decoding/frame-output integration is G10B.  GUI and
portable-package integration is G10C.

Required sequence:

G10A automatic inventory/control generation
-> G10B batch-aware discovery processing
-> G10C GUI/orchestrator/package integration
-> U002 automatic variable screening

Do not skip directly to U002 and do not continue final packaging of the old
six-signal-only behavior as if it were the completed U001 product.

