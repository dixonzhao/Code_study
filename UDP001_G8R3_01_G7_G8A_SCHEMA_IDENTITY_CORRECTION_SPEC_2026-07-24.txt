UDP001 G8R3 — GATE 7 AND GATE 8A CORRECTION SPECIFICATION
Output programs:

1. u001_output_package_assembler_v0_2.py.txt
2. u001_golden_replay_orchestrator_v0_2.py.txt

======================================================================
1. CHANGE CONTROL
======================================================================

This is a narrow correction of accepted v0.1 programs.

Allowed changes:

- final decoded Update schema completion;
- deterministic required-column order;
- identity-warning generation;
- Gate 8A selection of Gate 7 v0.2;
- formal SourceId correction to SOURCE_001;
- correction-specific self-tests and capsule fields.

Forbidden changes:

- CAN/DBC decoding;
- source row ordering;
- FrameCount assignment;
- semantic/validity classification;
- Frame View aggregation;
- cadence or hold logic;
- review-column selection;
- existing numerical values;
- thresholds or analysis algorithms.

======================================================================
2. GATE 7 v0.2 — FINAL UPDATE SCHEMA COMPLETION
======================================================================

Input source:

u001_output_package_assembler_v0_1.py

Output:

u001_output_package_assembler_v0_2.py.txt

ProgramName/version:

u001_output_package_assembler_v0_2.py
Version = 0.2

Preserve the existing CLI:

--self-test
--make-config
--build

----------------------------------------------------------------------
2.1 Required final Update columns
----------------------------------------------------------------------

The final u001_decoded_updates.csv must begin with these Data Contract-required
columns in exact order:

1. SourceRowIndex
2. SourceId
3. SourceType
4. ClockDomain
5. NativeTimestamp
6. NativeSequence
7. FrameCount
8. SyncKey
9. SyncQuality
10. FrameAssignmentStatus
11. FrameAssignmentAgeRows
12. CAN_ID
13. Channel
14. MessageName
15. CanonicalName
16. RawCode
17. PhysicalValue
18. ValueLabel
19. ValueKind
20. SignalRole
21. Valid
22. MissingReason
23. MessageCounter
24. SourceDateTime
25. SourceTimeUs

Existing noncontract implementation columns may follow these columns. Preserve
their relative upstream order. Reject duplicate column names.

Do not remove an existing Data Contract column. Do not rename a column by fuzzy
matching.

----------------------------------------------------------------------
2.2 Values for the three confirmed missing columns
----------------------------------------------------------------------

ClockDomain:

- create when absent;
- every row must contain exactly FRAMECOUNT_VIDEO;
- if already present, reject a nonblank value other than FRAMECOUNT_VIDEO.

NativeTimestamp:

- create when absent;
- write blank for every current-profile row;
- do not copy SourceDateTime or SourceTimeUs;
- do not treat the untrusted CAN timestamp as authoritative.

MessageCounter:

- create when absent;
- write blank for every current-profile row;
- do not infer it from a selected signal named MessageCounter;
- a future profile may populate it only after an explicit mapping decision.

The columns are schema-required. Blank values in the last two are an explicit
current-profile limitation, not missing columns.

----------------------------------------------------------------------
2.3 Streaming and preservation
----------------------------------------------------------------------

Rewrite u001_decoded_updates.csv into the final package by streaming.

Required invariants:

- input/output row count equal;
- SourceRowIndex order unchanged;
- SourceId values unchanged;
- all pre-existing cell strings preserved semantically;
- RawCode/PhysicalValue unchanged;
- FrameCount/SyncKey unchanged;
- Valid/MissingReason unchanged;
- no fill, interpolation, sorting, filtering, or inference;
- deterministic output for the same input/config;
- no complete 599970-row table required in memory.

Record in Quality Report global scope:

- update_required_column_missing_count
- clock_domain_bad_value_count
- native_timestamp_nonblank_count
- message_counter_nonblank_count
- update_row_count_before_schema_completion
- update_row_count_after_schema_completion

All must be zero except the two row counts, which must be equal to the decoded
Update row count.

----------------------------------------------------------------------
2.4 Identity warning generation
----------------------------------------------------------------------

Allowed IdentityStatus values in v0.2:

- PROVISIONAL_SMOKE
- CONTROLLED_REPLAY

Before writing the final manifest:

1. preserve nonidentity warnings;
2. remove any existing warning whose normalized value begins
   IdentityStatus-;
3. if IdentityStatus is PROVISIONAL_SMOKE, add exactly:

   IdentityStatus-PROVISIONAL_SMOKE

4. if IdentityStatus is CONTROLLED_REPLAY, add no IdentityStatus warning;
5. reject unsupported IdentityStatus;
6. reject a duplicate identity warning.

Internal consistency:

- PROVISIONAL_SMOKE + its exact warning = consistent;
- CONTROLLED_REPLAY + no IdentityStatus warning = consistent;
- all other combinations = blocking failure.

Write:

- IdentityStatus
- IdentityWarningConsistency = 1

to the final manifest.

----------------------------------------------------------------------
2.5 Gate 7R3 capsule
----------------------------------------------------------------------

Keep the existing Gate 7 capsule evidence and add:

H-Mm-Cc-Nn-Gg

where:

- M = missing required Update columns
- C = ClockDomain bad values
- N = NativeTimestamp nonblank values
- G = MessageCounter nonblank values

Add:

I-Ss-Ww

where:

- S = supported IdentityStatus, 1 yes
- W = status/warning consistency, 1 yes

C-0 requires:

M0 C0 N0 G0 S1 W1.

----------------------------------------------------------------------
2.6 Gate 7 v0.2 self-tests — exactly 20
----------------------------------------------------------------------

Preserve the intent of all 16 v0.1 tests and add/extend coverage so the final
suite contains exactly 20 deterministic tests:

T01–T16: accepted v0.1 behavior.
T17: add the three absent required columns and produce exact required order.
T18: ClockDomain fixed value; NativeTimestamp/MessageCounter remain blank.
T19: PROVISIONAL_SMOKE creates exactly its matching warning.
T20: CONTROLLED_REPLAY removes stale identity warnings and creates none.

Required output:

UDP001-G7R3-SELFTEST-PASS-20

======================================================================
3. GATE 8A v0.2 — CORRECTED REPLAY ORCHESTRATION
======================================================================

Input source:

u001_golden_replay_orchestrator_v0_1.py

Output:

u001_golden_replay_orchestrator_v0_2.py.txt

ProgramName/version:

u001_golden_replay_orchestrator_v0_2.py
Version = 0.2

Preserve the existing CLI:

--self-test
--make-config
--run

----------------------------------------------------------------------
3.1 Formal module list
----------------------------------------------------------------------

Use the accepted v0.1 modules for Gates 1–6 and require:

Gate 7:

u001_output_package_assembler_v0_2.py

Do not silently fall back to:

u001_output_package_assembler_v0_1.py

Reject duplicate or ambiguous module discovery.

----------------------------------------------------------------------
3.2 Formal replay identity
----------------------------------------------------------------------

Generated replay config must use:

DatasetId      = UDP001_GOLDEN_REPLAY_G8R3
RunId          = UDP001_G8R3_REPLAY_001
SegmentId      = SEGMENT_001
SourceId       = SOURCE_001
IdentityStatus = CONTROLLED_REPLAY

SourceId identifies the source, not the run. It must equal the corrected
reference SourceId.

After Gate 7 v0.2 build, require:

- final SourceId = SOURCE_001;
- final IdentityStatus = CONTROLLED_REPLAY;
- no IdentityStatus-PROVISIONAL_SMOKE warning;
- IdentityWarningConsistency = 1;
- final decoded Update required-column-missing count = 0.

----------------------------------------------------------------------
3.3 Protected hashes
----------------------------------------------------------------------

Preserve all v0.1 pre/post immutability checks.

The orchestrator must still report protected-hash equality for all seven
stages. The new Gate 7 package differs from the old v0.1 package by approved
schema/manifest correction, but the replayed v0.2 package must be deterministic
against the separately built corrected v0.2 reference package.

Do not use an old Gate 7 v0.1 package as the corrected reference.

----------------------------------------------------------------------
3.4 Gate 8AR3 capsule
----------------------------------------------------------------------

Preserve existing G8A evidence and add:

G7-V2-H0-I1

where:

- V2 = Gate 7 v0.2 selected;
- H0 = final decoded Update missing required columns = 0;
- I1 = candidate identity internally consistent.

C-0 requires all seven stage C codes 0, all seven protected hashes equal, V2,
H0, and I1.

----------------------------------------------------------------------
3.5 Gate 8A v0.2 self-tests — exactly 20
----------------------------------------------------------------------

Preserve the intent of all 16 v0.1 tests and add/extend coverage:

T01–T16: accepted orchestration behavior.
T17: exact Gate 7 v0.2 discovery; v0.1 fallback rejected.
T18: generated same-source replay config uses SOURCE_001.
T19: CONTROLLED_REPLAY final warning consistency is checked.
T20: final decoded Update header completeness is checked.

Required output:

UDP001-G8AR3-SELFTEST-PASS-20

======================================================================
4. IMPLEMENTATION QUALITY
======================================================================

- Use sys.executable and subprocess argument lists, never shell=True.
- Do not add third-party dependencies beyond those already used by v0.1.
- Keep input hashes before/after.
- Reject existing nonempty output folders.
- Store no company absolute paths in final manifests/capsules.
- Never modify the supplied v0.1 files.
