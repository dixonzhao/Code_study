UDP002 U22B13A 执行指南
版本：v0.2
目的：验收完整 Candidate Inventory、Module ReviewTrace、单一 ReviewMemo 及英/日模板回退契约。

一、文件放置

1. 下载并解压 UDP002_U22B13A_PACKAGE_v0_2_2026-08-25.zip。
2. 将解压得到的5个文件直接放入：
   C-CODE\U-UDP002\04_TEMP
3. 不要建立子文件夹；保留 .py.txt 后缀。

二、CMD执行

从 C-CODE 根目录只执行以下三条命令：

1. PREPARE
python U-UDP002\04_TEMP\u002_u22b13a_prepare_v0_1.py.txt --workspace-root U-UDP002

2. SELF-TEST
python U-UDP002\04_TEMP\u002_u22b13a_review_contract_qualification_v0_2.py.txt --self-test

3. EXECUTE
python U-UDP002\04_TEMP\u002_u22b13a_review_contract_qualification_v0_2.py.txt --contract-root U-UDP002\04_TEMP --out U-UDP002\07_OUTPUT\U22B13A

PREPARE已经负责：
- 5个当前文件的存在性检查；
- 4个冻结依赖文件的SHA-256检查；
- 旧B13A TEMP文件归档；
- 旧B13A输出隔离；
- 未知TEMP文件保留；
- 输出目录创建；
- Prepare receipt和诊断生成。

不要再手动执行 mkdir、dir、if exist、type、hash或清理命令。

三、成功标准

PREPARE：
B13AP N-F5-S8-A*-O*-M*-U*-X0 C-0

SELF-TEST：
UDP002-U22B13A-SELFTEST-PASS-20 C-0

EXECUTE：
B13A N-S2-T1-A1-L2-E1-J1-F1-P3-X0 C-0

星号位置取决于公司04_TEMP与旧输出状态，不要求固定数值。

四、需要回传

正常完成时只回传EXECUTE最后一行：
B13A N-S2-T1-A1-L2-E1-J1-F1-P3-X0 C-0

若任一步C-7，立即停止并回传该C-7行；不要手动移动或修改失败证据。

五、本节点边界

- 不读取公司CAN数据；
- 不修改现有U001/U002 Run；
- 不安装Module；
- 不修改冻结的U003 v0.8；
- 不宣称Radar故障或Root Cause。

