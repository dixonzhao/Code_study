UDP001 G10D2A - PORTABLE RUNTIME HOST v0.5
Date: 2026-07-29
Status: IMPLEMENTATION AUTHORIZED; PACKAGE BUILD FORBIDDEN UNTIL HOST CG1 PASS

======================================================================
1. CONTEXT
======================================================================

The final development active set has passed:

G10D1 N-A12-C4-P13-M0-E0-B13-R0-X0 C-0
BM-PFFF-SFFF-TFFF-K0-O11

Twelve formal programs are healthy.  GUI v0.3 remains only as a development
fallback.  The old portable sources are obsolete because they know none of
the four G10A-G10C additions and still assume GUI v0.3 / nine programs.

This task updates only the Portable Runtime Host.  It does not update the
Builder, build an EXE, edit U001 algorithms or run company Case data.

======================================================================
2. COPILOT INPUT AND OUTPUT
======================================================================

Open a fresh Company Copilot chat and attach exactly these three files:

1. accepted archived u001_portable_runtime_host_v0_4.py
2. accepted u001_discovery_controller_v0_2.py
3. this specification

If the archived host path is unknown, locate it from C-CODE CMD with:

dir /s /b .\UDP001\u001_portable_runtime_host_v0_4.py

Attach the exact v0.4 source, not an EXE or generated launcher.

Return one actual downloadable file named exactly:

u001_portable_runtime_host_v0_5.py.txt

Do not return only a code block.  Do not add spaces, parentheses, `download`
or `下载` to the filename.

After download, remove only the final `.txt` and save initially as:

C-CODE\UDP001\04_TEMP\u001_portable_runtime_host_v0_5.py

Keep v0.4, Controller v0.2 and all accepted programs unchanged.

======================================================================
3. FINAL RELEASE PROGRAM ALLOWLIST
======================================================================

Expose this exact tuple in this exact order:

APPROVED_PROGRAMS = (
    "u001_foundation_core_v0_1.py",
    "u001_selected_update_decoder_v0_1.py",
    "u001_framecount_assignment_v0_1.py",
    "u001_semantic_validity_classifier_v0_1.py",
    "u001_framecount_view_builder_v0_1.py",
    "u001_signal_cadence_bounded_hold_v0_1.py",
    "u001_output_package_assembler_v0_3.py",
    "u001_g8a_thin_orchestrator_v0_5.py",
    "u001_auto_discovery_profile_builder_v0_2.py",
    "u001_g10b_single_pass_discovery_engine_v0_5a.py",
    "u001_discovery_controller_v0_2.py",
    "u001_data_preparation_workbench_gui_v0_4a.py",
)

GUI v0.3 is not in the release allowlist.  Validation probes, package builders,
old versions and development Gates are not in the release allowlist.

Constants:

PROGRAM_VERSION = "0.5"
DEFAULT_GUI_NAME = "u001_data_preparation_workbench_gui_v0_4a.py"
SELFTEST_TOKEN = "U001-PORTABLE-HOST-V05-SELFTEST-PASS-24"

======================================================================
4. IMPORT-SAFE RELEASE CONTRACT
======================================================================

Expose:

get_portable_host_contract() -> dict

Return exactly these top-level keys:

Version
ApprovedPrograms
DefaultGUI
ProgramDirectory
DispatchModes

Values:

Version = "0.5"
ApprovedPrograms = list(APPROVED_PROGRAMS)
DefaultGUI = DEFAULT_GUI_NAME
ProgramDirectory = "01_PROGRAMS"
DispatchModes = ["direct_script", "host_program", "run_gui",
                 "list_programs", "hosted_self_test_all"]

Importing the host must not create a GUI, run a program or mutate files.

======================================================================
5. PATH APPROVAL
======================================================================

Expose:

resolve_approved_program(requested_path, portable_root=None) -> pathlib.Path

Rules:

- resolve absolute/canonical path before approval;
- basename must be in APPROVED_PROGRAMS;
- resolved parent must be the exact 01_PROGRAMS directory of the inferred or
  supplied portable root;
- an approved basename outside that directory is rejected;
- an unapproved basename inside that directory is rejected;
- missing files are rejected;
- do not approve by basename alone;
- never search the development venv or current working directory as fallback.

When portable_root is absent, infer it only when the requested file's parent
is named exactly 01_PROGRAMS; the inferred root is that parent's parent.

All rejection paths return nonzero and emit a concise `U001HOST-ERROR:` line.

======================================================================
6. HOSTED EXECUTION SEMANTICS
======================================================================

Preserve the accepted v0.4 runpy hosting approach and its lessons:

- insert exact 01_PROGRAMS at the front of sys.path during hosted execution;
- set cwd to exact 01_PROGRAMS;
- set sys.argv to [program_path] + program_arguments;
- run with runpy.run_path(..., run_name="__main__");
- convert SystemExit(None/0) to exit 0 and nonzero SystemExit to nonzero;
- restore sys.argv, cwd and sys.path after execution;
- do not use a copied venv;
- do not invoke an installed external Python interpreter.

Expose:

dispatch_hosted_program(program_path, program_args, portable_root=None) -> int

The frozen host must support Controller/GUI subprocess calls where
sys.executable is the Runtime Host EXE.  Therefore support both entry forms:

1. Direct-script form:

U001_RuntimeHost.exe <approved-program.py> [program arguments...]

2. Explicit form:

U001_RuntimeHost.exe --host-program <approved-program.py> -- [program arguments...]

The source form used by independent tests is equivalent:

python u001_portable_runtime_host_v0_5.py <approved-program.py> [args]

Do not require Controller v0.2 or GUI v0.4a to know that they are frozen.

======================================================================
7. HOST COMMANDS
======================================================================

Required commands:

--self-test
    deterministic source self-test; no GUI opened.

--list-programs
    print the 12 approved basenames in exact order and exit 0.

--host-program <path> -- [args]
    explicit hosted dispatch.

--run-gui [--portable-root <root>]
    resolve DEFAULT_GUI_NAME under exact 01_PROGRAMS and host it.

--hosted-self-test-all [--portable-root <root>]
    execute `<program> --self-test` for all 12 approved programs in order,
    record each exit/token, stop on first failure and emit a compact final line.

For the frozen EXE, derive its default portable root from the installed layout,
not from the development source path.  Preserve compatible v0.4 frozen-root
resolution where correct.

======================================================================
8. DEPENDENCY AND SECURITY BOUNDARY
======================================================================

The Runtime Host supplies the interpreter.  Program source files remain
external and hashable under UDP001/01_PROGRAMS.

The Host must not:

- contain CAN decode, discovery, baseline, anomaly or U002 logic;
- modify child source;
- weaken Controller v0.2 path/identity checks;
- permit arbitrary Python scripts;
- include GUI v0.3 in the release allowlist;
- hard-code a company absolute path;
- silently fall back to development Python.

======================================================================
9. SELF-TEST
======================================================================

Add deterministic tests for at least:

- exact 12-program allowlist/order;
- exact default GUI;
- import safety;
- approved exact 01_PROGRAMS path;
- approved basename outside 01_PROGRAMS rejected;
- unapproved basename inside 01_PROGRAMS rejected;
- missing program rejected;
- direct-script argument recognition;
- explicit host-program argument recognition;
- sys.argv/cwd/sys.path setup and restoration;
- zero and nonzero SystemExit propagation;
- no old GUI v0.3 literal in the allowlist.

Required final line:

U001-PORTABLE-HOST-V05-SELFTEST-PASS-24

A larger count is acceptable.

From C-CODE CMD run:

python .\UDP001\04_TEMP\u001_portable_runtime_host_v0_5.py --self-test

Then stop and run the separate G10D2ACG1 independent Gate supplied by Codex.
Do not update the Builder or build a package before CG1 passes.

END OF G10D2A SPECIFICATION
