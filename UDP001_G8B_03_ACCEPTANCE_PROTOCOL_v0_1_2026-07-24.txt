UDP001 — GATE 8B ACCEPTANCE PROTOCOL v0.1
Date: 2026-07-24

======================================================================
1. RETURN FORMAT
======================================================================

UDP001G8BR1
B1-abcd
B2-abcd
B3-abcd
B4-abcd
N-Rr-Cc-Ee-Ss-Mm-Uu-Ff-Xx
D1-Ee D2-Ee D3-Ss D4-Ss D5-Mm D6-Ee
C-n

For C-0, return no Memo.

For C-1 through C-9, add at most one short Memo.

======================================================================
2. B CODES
======================================================================

B1 — independent validator implementation

Positions:

1. self-test 16/16
2. one complete import-safe Python file
3. no Gate 1–Gate 8A import/execution/helper reuse
4. package inputs remained read-only

B2 — independent package validity

Positions:

1. exact six-file inventories
2. Update/Frame/Catalog/Quality/Manifest/Review schemas valid
3. independent row/order/type/policy checks pass
4. cross-file and path-safety checks pass

B3 — candidate-versus-reference equivalence

Positions:

1. D1/D2/D6 exact hashes equal
2. D3 Catalog semantic equality
3. D4 Quality semantic equality
4. D5 approved-field-only normalized manifest equality

B4 — evidence and report safety

Positions:

1. orchestration evidence structurally valid but does not bypass data checks
2. pre/post package hashes equal
3. exactly three deterministic validation outputs
4. zero blocking validation errors

PASS requires every B position = 2.

======================================================================
3. N AND D LINES
======================================================================

N-Rr-Cc-Ee-Ss-Mm-Uu-Ff-Xx

- R reference package file count
- C candidate package file count
- E exact-equality count for D1/D2/D6
- S semantic-equality count for D3/D4
- M normalized manifest equality
- U Update Stream rows
- F Frame View rows
- X blocking errors

D mapping:

- D1 decoded updates, E exact
- D2 Frame View, E exact
- D3 Signal Catalog, S semantic
- D4 Quality Report, S semantic
- D5 Run Manifest, M normalized
- D6 Review View, E exact

Current PASS:

N-R6-C6-E3-S2-M1-U599970-F20617-X0
D1-E1 D2-E1 D3-S1 D4-S1 D5-M1 D6-E1

======================================================================
4. RESULT CODES
======================================================================

C-0 = PASS
C-1 = generated source incomplete or import/syntax failure
C-2 = self-test/independence failure
C-3 = package inventory/symlink/path-escape failure
C-4 = independent schema/count/order/policy failure
C-5 = Update/Frame/Review exact-equality failure
C-6 = Catalog/Quality semantic-equality failure
C-7 = Run Manifest normalized/path-safety failure
C-8 = orchestration evidence/input mutation/report-output failure
C-9 = other blocking failure; add one short Memo

======================================================================
5. ACCEPTANCE MEANING
======================================================================

C-0 proves that the Gate 8A candidate reproduces the accepted Gate 7 reference
at the Data Contract boundary, subject only to explicitly approved run
identity/timestamp/duration differences in the Run Manifest.

After C-0:

- freeze the accepted U001 v0.1 smoke implementation;
- retain legacy P001–P003 for comparison and fallback;
- do not yet delete or retire legacy tools;
- create a separate expansion experiment for more signals/full-log scale;
- begin U002 only through a new Algorithm/Experiment decision, not by adding
  analysis logic to U001.
