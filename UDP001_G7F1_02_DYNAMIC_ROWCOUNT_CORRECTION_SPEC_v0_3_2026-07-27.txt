UDP001 G7F1 — OUTPUT PACKAGE ASSEMBLER v0.3 CORRECTION SPECIFICATION
Date: 2026-07-27
Target output: u001_output_package_assembler_v0_3.py

======================================================================
1. CHANGE BOUNDARY
======================================================================

This is a narrow source-size-independence correction to the accepted Gate 7
v0.2 implementation.

Preserve:

- the three CLI modes: --self-test, --make-config, --build;
- exact required-input discovery and ambiguity rejection;
- the six-file package schema and filenames;
- Data Contract fields and semantics;
- binary/semantic preservation requirements;
- SourceId and IdentityStatus rules;
- atomic output behavior;
- failure on missing, duplicate, mutated, or invalid inputs;
- compact return-capsule behavior;
- all accepted v0.2 checks not explicitly changed below.

Do not modify or implement:

- CAN decode;
- frame assignment;
- semantic validity classification;
- Frame View construction;
- hold policy;
- camera/radar selection;
- baseline, residual, event, or SysID analysis;
- G8A2 orchestration;
- GUI behavior;
- the Data Contract or final six-file schema.

======================================================================
2. DEFECT TO REMOVE
======================================================================

The production implementation must not require the historical bounded-smoke
row counts:

- decoded Update rows = 599970;
- Frame View rows = 20617.

These values were observations from one bounded acceptance Run, not universal
Data Contract invariants.

No production validation, build path, default config, or return decision may
use either literal as an expected row count.

Signal Catalog count, IncludeInFrameView count, and Review View width remain
profile/config evidence and must not be confused with source-size row counts.

======================================================================
3. MAKE-CONFIG REQUIREMENTS
======================================================================

During --make-config:

1. retain exact-one discovery for every required filename;
2. identify the accepted Gate 4 semantic-validity Update CSV used as the final
   decoded Update source;
3. identify the accepted Gate 6 applied Frame View CSV used as the final Frame
   View source;
4. inspect both CSVs by streaming; do not load either complete table in memory;
5. record for each source artifact:
   - local path;
   - filename;
   - byte size;
   - SHA-256;
   - exact header sequence;
   - observed data-row count excluding the header;
6. store the observed counts in explicit dynamic config fields equivalent to:
   - ExpectedUpdateRows;
   - ExpectedFrameRows;
7. retain all other current v0.2 config and identity fields;
8. write deterministic UTF-8 JSON atomically.

The field names may follow the existing v0.2 config structure, but their
values must be derived from the discovered current artifacts, never from a
smoke-run constant.

======================================================================
4. BUILD REQUIREMENTS
======================================================================

Before package creation, --build must:

1. validate config structure and supported program/config version;
2. re-resolve every configured required input;
3. verify each source artifact still matches its config SHA-256 and byte size;
4. stream-count the current Update and Frame View inputs;
5. require those counts to equal the dynamic config observations;
6. reject any artifact mutation after --make-config;
7. retain every existing schema, order, validity, identity, and package check.

During package creation:

- the decoded Update output row count must equal ExpectedUpdateRows;
- the Frame View output row count must equal ExpectedFrameRows;
- Review View row count must equal the Frame View row count unless the existing
  accepted v0.2 rule explicitly defines a narrower reviewed subset;
- no row may be added, removed, filled, inferred, or reordered by this fix;
- preserve streaming behavior and bounded memory use;
- preserve exact six-file package output.

Do not silently trust a manually edited count. SHA-256, byte size, and observed
row count must be checked together.

======================================================================
5. QUALITY AND MANIFEST EVIDENCE
======================================================================

The final quality report and manifest must record the actual current Run
counts. They must not describe the current Run as the historical smoke Run.

If the existing v0.2 implementation already records:

- update_row_count_before_schema_completion;
- update_row_count_after_schema_completion;
- Frame View row count;
- Review View row count;

retain those metrics and populate them dynamically.

The two update counts must equal each other and the current Update source row
count. Frame/Review counts must follow the accepted v0.2 relationship.

Do not invent a new equivalence whitelist or normalize away a real data
difference.

======================================================================
6. VERSION AND CAPSULE
======================================================================

Program filename:

u001_output_package_assembler_v0_3.py

Program version:

0.3

Self-test success token:

UDP001-G7V03-SELFTEST-PASS-24

Build capsule header:

UDP001G7V03R1

Preserve the compact evidence fields equivalent to:

N-Uu-Rr-Ss-Kk-Vv-Ww-Ff-Xx
C-n

U, R, and V are dynamic observed counts. Passing must not require historical
U=599970 or R=20617.

======================================================================
7. REQUIRED SELF-TESTS
======================================================================

Retain all 20 accepted v0.2 self-tests and add at least these four tests for a
total of exactly 24:

T21 Dynamic small package A:
- nonhistorical Update and Frame row counts;
- make-config derives the counts;
- build passes and reports the derived values.

T22 Dynamic small package B:
- different counts from T21;
- proves counts are not shared, cached, or hard-coded;
- build passes with the second derived pair.

T23 Mutation after config:
- modify one configured source after --make-config;
- build must reject it through hash/size/count evidence;
- no passing package may remain.

T24 Count mismatch rejection:
- make config from a valid artifact;
- present a changed-row artifact at build time;
- build must fail with a concise controlled error.

Self-tests must use temporary synthetic data only and must not require company
files, pandas, Excel, network access, or a GUI.

======================================================================
8. FULL-SOURCE ACCEPTANCE
======================================================================

For the preserved failed full-source Run:

- ExpectedUpdateRows must be derived as 1647102;
- ExpectedFrameRows must be derived from the current Gate 6 Frame View output;
- the Gate 7 build must produce exactly six final package files;
- the capsule must report dynamic U/R/V values, F6, X0, C-0;
- all existing preservation, schema, validity, and identity checks must pass.

Do not hard-code 1647102 either. It is acceptance evidence for this Run, not a
new universal constant.

END
