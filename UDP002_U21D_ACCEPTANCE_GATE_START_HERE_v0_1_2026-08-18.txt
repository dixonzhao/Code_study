UDP002 U21D INDEPENDENT BLACK-BOX ACCEPTANCE GATE

Task ID: U21D-GATE
Version: 0.1
Date: 2026-08-18
Owner: Zhao Zhili
Status: APPROVED FOR COMPANY-SIDE IMPLEMENTATION


1. PURPOSE

Independently evaluate the candidate:

  u002_qualified_module_plan_resolver_v0_1.py.txt

The Gate must not trust U21D1 self-reported counts. It must construct external
Module Packs, obtain real activation evidence through the frozen U21C
controller, call only the U21D public CLI, and independently inspect the
resulting plan and failure behavior.

No production Module callable or company data may be used.


2. GENERATE EXACTLY ONE FILE

Generate and return:

  u002_u21d_acceptance_gate_v0_1.py.txt

Initial location:

  U-UDP002/04_TEMP

Do not modify the candidate resolver, Registry, Activation Controller or frozen
contracts. Do not return only prose or partial code.


3. REQUIRED MODES

Gate helper self-test:

  --self-test

Success:

  U21DGS1 N-T12-X0 C-0

Formal run:

  --run
  --resolver PATH
  --registry-program PATH
  --activation-controller PATH
  --schema-bundle PATH
  --contract-validator PATH
  --fixture-manifest PATH
  --out-dir PATH

The formal out-dir must not already exist. Controlled exits are 0 and 7.


4. GATE-OWNED OUTPUT

Create below --out-dir:

  01_FIXTURES
  02_ACTIVATION_EVIDENCE
  03_PLAN_OUTPUTS
  04_GATE_EVIDENCE
  05_LOGS

Final evidence:

  04_GATE_EVIDENCE/U21D_ACCEPTANCE_EVIDENCE.json

All Gate writes remain inside --out-dir or Gate-owned synthetic Module Packs.


5. INPUT IDENTITIES

Record exact byte SHA-256 for:

- candidate resolver;
- frozen Registry program;
- frozen Activation Controller;
- Schema Bundle;
- Validator;
- accepted fixture Manifest.

The final Gate decision applies only to the tested resolver SHA-256.


6. VALID SYNTHETIC SOFTWARE PLAN

Construct four authorized Python Module Packs with unique identities:

M1 S1 semantic candidate builder
  provides SYNTHETIC_SEMANTIC_CANDIDATE, non-exclusive.

M2 S1 arbitration
  requires SYNTHETIC_SEMANTIC_CANDIDATE ONE_OR_MORE;
  provides SYNTHETIC_CANONICAL_SEMANTIC, exclusive.

M3 S4 baseline
  requires SYNTHETIC_CANONICAL_SEMANTIC ONE;
  provides SYNTHETIC_BASELINE, exclusive.

M4 S7 exporter
  requires SYNTHETIC_BASELINE ONE;
  provides SYNTHETIC_HANDOFF, exclusive.

Each implementation:

- passes only its declared --self-test;
- writes a self-test marker;
- writes a production marker if invoked without --self-test.

Create real configuration files and exact Manifest hashes. Use the frozen U21C
controller CLI to produce one accepted activation evidence record for M1-M4.


7. POSITIVE REPEATABILITY

Resolve the same four-Module selection twice into separate plan files.

Independently require:

- both resolver exits are 0;
- both PlanStatus values are SOFTWARE_PLAN_READY;
- SelectedModuleCount == 4;
- execution order is M1, M2, M3, M4 after Stage/dependency ordering;
- all Registry, Manifest, implementation, configuration and activation-evidence
  hashes equal independent Gate calculations;
- ArtifactGraph has the three required dependency transitions;
- SignalBindingStatus == DEFERRED_TO_RUN_PREFLIGHT;
- unresolved RequiredInputs/OptionalInputs are preserved;
- no production marker exists.

Compare a stable projection of the two plans. Exclude only GeneratedAtUtc and
RegistrySnapshotSha256 when Registry timestamps differ. All other governed
structure must agree.


8. INDEPENDENT FAILURE MATRIX

Create and run at least these eight denied requests:

F1 missing activation evidence;
F2 stale implementation hash in activation evidence;
F3 requested EngineVersion different from evidence;
F4 unsatisfied mandatory Artifact requirement;
F5 duplicate selected identity;
F6 exclusive output collision;
F7 provider exists only in a later Stage;
F8 duplicate OutputNamespace.

Independently require exit 7, the expected stable reason and no partial
SOFTWARE_PLAN_READY output.

The Gate may include an artifact-cycle test as additional evidence, but it must
not replace any F1-F8 case.


9. ACTIVATION-EVIDENCE SELECTION

Create two valid accepted evidence records for one exact identity with different
GeneratedAtUtc values and paths. Require deterministic selection of the newer
record and a superseded-evidence warning.

Create invalid/failed JSON evidence in the same directory. Require it never to
qualify a Module and never to crash resolution.


10. NON-EXECUTION PROOF

Search every Gate fixture after all tests:

- all expected self-test markers must exist;
- production marker count must be zero.

This proves U21D resolves metadata/evidence only and does not execute production
Module callables.


11. FINAL GATE EVIDENCE

Write one atomic UTF-8 JSON object containing:

  GateType = U21D_INDEPENDENT_ACCEPTANCE
  GateVersion = 0.1
  GeneratedAtUtc
  GateStatus
  InputHashes
  PositiveRepeatability
  FailureMatrix
  EvidenceSelectionResult
  ArtifactGraphResult
  ProductionMarkerCount
  Assertions
  ReturnCapsule

Assertions must be independent Gate observations rather than copied U21D
summary counts.


12. SUCCESS CAPSULE

Print only:

  U21DG1 N-R2-M4-O4-F8-E2-P1-X0 C-0

Meanings:

  R2 = two positive plan resolutions passed
  M4 = four exact qualified Modules joined
  O4 = deterministic four-Module order passed
  F8 = eight independent plan denials passed
  E2 = multi-evidence selection and invalid-evidence isolation passed
  P1 = no production callable executed
  X0 = no uncontrolled exception

Failure:

  U21DG1-ERROR-<STABLE_REASON> C-7


13. FILES TO PROVIDE TO THE IMPLEMENTATION ASSISTANT

Provide exactly:

1. this Gate START_HERE document;
2. candidate u002_qualified_module_plan_resolver_v0_1.py.txt;
3. accepted u002_module_activation_controller_v0_1.py.

The Registry and fixture are supplied through CLI paths. Read all three files
completely before generating the Gate.


14. USER EXECUTION FROM C-CODE ROOT USING WINDOWS CMD

Step 1:

  python .\U-UDP002\04_TEMP\u002_u21d_acceptance_gate_v0_1.py.txt --self-test

Step 2:

  python .\U-UDP002\04_TEMP\u002_u21d_acceptance_gate_v0_1.py.txt --run --resolver .\U-UDP002\04_TEMP\u002_qualified_module_plan_resolver_v0_1.py.txt --registry-program .\U-UDP002\01_PROGRAMS\u002_module_registry_v0_1.py --activation-controller .\U-UDP002\01_PROGRAMS\u002_module_activation_controller_v0_1.py --schema-bundle .\U-UDP002\04_TEMP\u002_core_contract_schema_bundle_v0_1.json.txt --contract-validator .\U-UDP002\04_TEMP\u002_core_contract_validator_v0_1.py.txt --fixture-manifest .\U-UDP002\05_TESTS\01_FIXTURES\U20A_v0_1\valid_module_manifest.json --out-dir .\U-UDP002\05_TESTS\02_ACCEPTANCE\U21D_GATE_20260818

If the output directory exists, use a new suffix such as _R2. Never overwrite
prior Gate evidence.


15. RETURN

On success return only:

  U21DG1 N-R2-M4-O4-F8-E2-P1-X0 C-0

On failure return the first complete ERROR line and at most one short Memo.


16. ACCEPTANCE BOUNDARY

Gate PASS authorizes freeze/promotion only for the exact resolver SHA-256 stored
in U21D_ACCEPTANCE_EVIDENCE.json. It does not authorize real-data execution or
complete U21E signal binding.


END
