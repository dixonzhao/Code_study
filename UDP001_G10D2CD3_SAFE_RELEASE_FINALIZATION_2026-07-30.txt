UDP001 G10D2C-D3 — SAFE RELEASE FINALIZATION WITHOUT REBUILD
Date: 2026-07-30
Status: RECOVERY COPY AUTHORIZED; EXISTING LAUNCHER PROMOTION STILL FORBIDDEN

Accepted staged release validation:

G10D2CD2 N-R1-S1-G1-E0-X0 C-0

The Stage-6 package is complete and independently valid. The original build
failed only when Builder v0.7 tried to move the whole release_stage directory
to the requested output path. Do not rebuild.

Download:

u001_g10d2cd3_safe_release_finalizer_v0_1.py.txt

Remove only the final .txt and place the Python file in:

C-CODE\UDP001\04_TEMP

Keep the original G10D2C Gate in the same folder:

u001_g10d2c_portable_release_acceptance_gate_v0_1.py

From C-CODE Windows CMD confirm the requested output still does not exist:

if exist .\UDP001\04_TEMP\G10D2C_RELEASE_01 (echo STOP-OUTPUT-EXISTS) else (echo OUTPUT-READY)

Continue only when OUTPUT-READY is printed. Then run:

python .\UDP001\04_TEMP\u001_g10d2cd3_safe_release_finalizer_v0_1.py --udp001-root .\UDP001 --out .\UDP001\04_TEMP\G10D2C_RELEASE_01

The finalizer:

1. locates the accepted latest PK04 release_stage;
2. requires the prior D2 Gate PASS evidence;
3. creates the final output with an INCOMPLETE marker;
4. copies every file with bounded retry rather than renaming the source tree;
5. compares relative-file inventory and byte sizes;
6. verifies SHA-256 for the ZIP, EXE, manifest, capsule, 12 programs and 4 controls;
7. runs the original independent Gate against the copied output;
8. removes the INCOMPLETE marker only after Gate C-0.

It never deletes or modifies the original PK04 release_stage. It never
overwrites an existing output. Console progress is printed periodically.

Expected success:

G10D2CD3 N-R1-C1-V1-G1-E0-X0 C-0

On failure return only the complete G10D2CD3 line. Preserve both source stage
and any marked incomplete output for diagnosis.

After D3 C-0, perform the manual GUI smoke test:

start "" ".\UDP001\04_TEMP\G10D2C_RELEASE_01\U001_Portable_v0_2\UDP001\00_LAUNCHER\U001_Workbench.exe"

Confirm GUI opens, current v0.4a controls are visible and closing is normal.

Return exactly two lines on complete success:

G10D2CD3 N-R1-C1-V1-G1-E0-X0 C-0
G10D2CM1-GUI-OPEN-OK C-0

Do not overwrite UDP001\00_LAUNCHER and do not delete 04_TEMP artifacts. A
separate G10D2D node will archive release tooling and define second-PC acceptance.

