UDP002 U21F GATE DENIAL INDEX-2 ISOLATED REPLAY

Task ID: U21F-GATE-DIAG-02
Version: 0.1
Date: 2026-08-19
Owner: Zhao Zhili
Status: APPROVED FOR ONE READ-ONLY ISOLATED REPLAY


1. SESSION START

Use a fresh Copilot chat. Do not continue the exhausted previous chat.

Confirmed project state:

- U21A-U21E are accepted, frozen and promoted;
- unchanged U21F candidates passed internal self-test:

    U21F1 N-T36-R3-M12-A10-F10-I8-X0 C-0

- independent Gate v0.1 failed with K-DENIAL;
- retained diagnosis identified denial index 2, missing Engine, but did not
  retain the child RC/stdout/stderr or prove before/after hash equality;
- no U21F source or Gate correction is authorized yet.


2. PROVIDE TO COPILOT

Provide exactly these three files:

1. this complete instruction;
2. exact u002_u21f_acceptance_gate_v0_1.py.txt;
3. exact u002_controlled_stage_executor_v0_1.py.

Tell Copilot that the other five valid dependency paths already exist on the
company computer and will be supplied only as runtime CLI arguments.


3. GENERATE EXACTLY ONE FILE

Generate and return without Markdown fences:

  u002_u21f_gate_index2_probe_v0_1.py.txt

This is a standard-library, diagnosis-only program. It must not import the Gate,
Executor, Engine or validators as libraries.


4. PROBE CLI

Implement exactly:

  --self-test

and:

  --run
  --executor PATH
  --protocol-bundle PATH
  --protocol-validator PATH
  --engine PATH
  --schema-bundle PATH
  --contract-validator PATH
  --out-dir PATH

Exactly one action per invocation.


5. ISOLATED REPLAY LOGIC

For --run:

1. require all six supplied real inputs to exist, be regular non-symlink files;
2. require out-dir to be absent and create it atomically;
3. stream SHA-256 for all six real inputs before child execution;
4. define the missing Engine path as:

     <out-dir>/private_missing_input/missing_engine_v0_1.py

   Do not create that file;
5. invoke the exact Executor as a child process through sys.executable with:

     --self-test
     --engine <the deliberately absent path>
     --protocol-bundle <valid supplied path>
     --protocol-validator <valid supplied path>
     --schema-bundle <valid supplied path>
     --contract-validator <valid supplied path>

6. use a 30-second timeout;
7. redirect child stdout and stderr directly to Gate-owned files, not unbounded
   PIPE memory;
8. retain at most 262144 bytes from each stream and record original byte count;
9. classify the final nonempty stdout line as CAP-1 only if it is an exact U21F1
   C-7 capsule; substring-only C-7 is insufficient;
10. classify stderr as:

      ARGPARSE   usage/error without traceback
      TRACEBACK  traceback present
      CONTROLLED no traceback and CAP-1
      NONE       none of the above

11. stream SHA-256 for the same six real inputs after the child exits;
12. H-1 only when every before/after hash is identical;
13. write one deterministic evidence JSON atomically using basenames, role names,
    hashes, RC, timeout state, capsule classification and relative log paths;
14. never record absolute company paths or raw business data.

The probe must not rerun the full Gate and must not modify any tested file.


6. PROBE SELF-TEST

Use private temporary dummy programs to test:

- success and nonzero child RC capture;
- exact C-7 capsule classification;
- argparse and traceback classification;
- timeout classification;
- before/after hash equality and mutation detection;
- bounded file-backed stdout/stderr;
- deterministic evidence and no absolute path leakage;
- out-dir non-overwrite.

Success line:

  UDP002-U21F-IDX2-PROBE-SELFTEST-PASS-16 C-0

Do not return the self-test line when it passes.


7. COMPANY EXECUTION FROM C-CODE ROOT

Place the returned probe directly in U-UDP002/04_TEMP.

First:

  python ".\U-UDP002\04_TEMP\u002_u21f_gate_index2_probe_v0_1.py.txt" --self-test

Then:

  python ".\U-UDP002\04_TEMP\u002_u21f_gate_index2_probe_v0_1.py.txt" --run --executor ".\U-UDP002\04_TEMP\u002_controlled_stage_executor_v0_1.py" --protocol-bundle ".\U-UDP002\04_TEMP\u002_module_runtime_protocol_bundle_v0_1.json" --protocol-validator ".\U-UDP002\04_TEMP\u002_module_runtime_protocol_validator_v0_1.py" --engine ".\U-UDP002\01_PROGRAMS\u002_engine_skeleton_v0_1.py" --schema-bundle ".\U-UDP002\04_TEMP\u002_core_contract_schema_bundle_v0_1.json.txt" --contract-validator ".\U-UDP002\04_TEMP\u002_core_contract_validator_v0_1.py.txt" --out-dir ".\U-UDP002\05_TESTS\02_ACCEPTANCE\U21F_GATE_IDX2_REPLAY_20260819"


8. REQUIRED RETURN

Return only:

  U21FI2 RC-<integer|TO> CAP-<0|1> ERR-<ARGPARSE|TRACEBACK|CONTROLLED|NONE> H-<0|1> X-<0|1> C-0

Add one short Memo only if CAP-0, containing the final nonempty stdout/stderr
line truncated to 120 characters. X is 1 only for an uncontrolled probe error.

On probe failure:

  U21FI2 RC-NA CAP-0 ERR-NONE H-0 X-1 C-7 K-<reason>


9. DECISION RULE AFTER RETURN

- RC nonzero, CAP-1, CONTROLLED, H-1:
  Executor is correct; issue one narrow Gate evidence/predicate correction.

- RC nonzero, CAP-0, ARGPARSE, H-1:
  classify whether missing-input rejection is valid CLI behavior and correct only
  the Gate contract if compatible with the approved Executor CLI.

- TRACEBACK or H-0 with complete evidence:
  stop and evaluate a versioned Executor or environment defect.

No correction occurs inside this task.

END
