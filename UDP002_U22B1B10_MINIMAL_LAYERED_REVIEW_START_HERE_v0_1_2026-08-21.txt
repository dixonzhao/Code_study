UDP002 U22B1B10 最小分层人工复核包
版本：v0.1
日期：2026-08-21

一、目的

B1B9已经证明：

- 327.66屏蔽本身造成主要受保护窗口变化；
- timeout在此基础上只额外产生单行Lost，并恢复若干原本无效的行；
- 恢复行是否跟踪正确目标，机器无法裁决。

B1B10不再搜索新参数，只把真正需要人判断的位置压缩为U003可读复核包。
本步骤不修改U001、已安装S1 Module、原始数据或任何正式参数。

二、Required Working Set

把本次下载的两个文件直接放在：

U-UDP002\04_TEMP

同时确认同一目录已有以下三个文件：

1. u002_u22b1b7_32766_counterfactual_replay_v0_2.py.txt
2. u002_u22b1b8_controlled_timeout_sweep_v0_1.py.txt
3. u002_u22b1b9_layered_regression_attribution_v0_1.py.txt

本次新文件：

4. u002_u22b1b10_minimal_layered_review_package_v0_1.py.txt
5. UDP002_U22B1B10_MINIMAL_LAYERED_REVIEW_START_HERE_v0_1_2026-08-21.txt

不要改名，不要放入04_TEMP\A-Archive。

三、CMD自测

在C-CODE根目录打开CMD，运行：

python U-UDP002\04_TEMP\u002_u22b1b10_minimal_layered_review_package_v0_1.py.txt --self-test

成功时显示：

U22B1B10-ST N-T28-X0 C-0

自测C-0时不需要回传。

四、正式运行

python U-UDP002\04_TEMP\u002_u22b1b10_minimal_layered_review_package_v0_1.py.txt --workspace-root U-UDP002

只回传程序输出的最多三行机器码。

五、生成文件

程序自动找到当前B1B4 Run，并生成：

04_REVIEW_PACKAGE\u22b1b10_layered_human_review_queue_v0_1.csv
  - U003直接加载的最小事件队列；
  - 只包含发生mask变化、timeout reset或重新捕获的位置；
  - 自动合并相距不超过40 Frame的检查点；
  - 每个区间前后最多扩展20 Frame；
  - 最多10个复核事件，超过即fail-close。

05_EXPORTS\u22b1b10_layered_human_review_matrix_v0_1.csv
  - 详细对照表；
  - A = 当前Accepted结果；
  - B = 仅屏蔽327.66后的结果；
  - T3/T5/T10/T20 = 在B基础上加入对应timeout后的结果。

05_EXPORTS\u22b1b10_layered_plot_view_v0_1.csv
  - 人工画图用9列表；
  - FrameCount、CAM1、CAM3、A、B、T3、T5、T10、T20；
  - 无效Canonical值留空，不用327.66画连续线。

05_EXPORTS\u22b1b10_minimal_layered_review_summary_v0_1.json
  - 输入Hash、事件数、复核问题和DecisionStatus。

六、U003复核问题

每个事件只回答以下问题之一：

1. 视频显示的目标更符合A还是B？
2. timeout产生的单帧reset是否只是合理的状态边界？
3. timeout重新获得的数据是否跟踪了正确的可见目标？

此步骤结束前不得批准3/5/10/20中的任何timeout。

七、成功标准

- 主返回码C-0；
- 事件队列不超过10行；
- 详细表和9列画图表均生成；
- 源表Hash运行前后不变；
- DecisionStatus仍为HUMAN_REVIEW_REQUIRED_NO_PARAMETER_PROMOTION。

八、失败处理

发生C-7时，只回传该行。
详细原因自动写入：

U-UDP002\04_TEMP\u22b1b10_minimal_layered_review_diagnostic_v0_1.json

不要让Office 365 Copilot修改程序或自动选择timeout。
