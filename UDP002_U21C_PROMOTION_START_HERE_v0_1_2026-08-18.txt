UDP002 U21C ACCEPTED CONTROLLER PROMOTION

Task ID: U21C-PROMOTION
Version: 0.1
Date: 2026-08-18
Owner: Zhao Zhili
Status: APPROVED FOR COMPANY-SIDE GENERATION AND EXECUTION


1. PURPOSE

Promote only the exact candidate controller accepted by the independent U21C
Gate into the formal program directory, then rerun its complete internal
self-test after relocation.

Accepted Gate capsule:

  U21CG1 N-R2-A1-F4-B2-I3-P1-X0 C-0

This task must not modify the controller, Registry or frozen contracts.


2. PROVIDE EXACTLY THREE FILES TO THE IMPLEMENTATION ASSISTANT

1. this promotion START_HERE document;
2. candidate u002_module_activation_controller_v0_1.py.txt;
3. U21C_ACCEPTANCE_EVIDENCE.json produced by the passing formal Gate.

Read all three completely before generation.


3. GENERATE EXACTLY ONE FILE

Return the complete downloadable Windows CMD file:

  UDP002_U21C_PROMOTE_ACCEPTED_CONTROLLER_v0_1.cmd.txt

The file must initially be downloaded into:

  U-UDP002/04_TEMP

Do not return only prose, a code-written status or a partial code block.


4. ACCEPTED IDENTITY

Read the actual Gate evidence structure and locate the exact SHA-256 that the
Gate recorded for the tested candidate controller.

Embed that exact lowercase SHA-256 as EXPECTED_CONTROLLER_SHA256 in the CMD
file. Do not ask the user to type or copy the SHA-256.

The CMD must also require from the Gate evidence:

  GateStatus = PASS
  ReturnCapsule = U21CG1 N-R2-A1-F4-B2-I3-P1-X0 C-0

Use the exact field path present in the provided evidence. Do not invent a
different evidence schema.


5. FIXED COMPANY PATHS

Run from C-CODE root using Windows CMD.

Source candidate:

  .\U-UDP002\04_TEMP\u002_module_activation_controller_v0_1.py.txt

Formal destination:

  .\U-UDP002\01_PROGRAMS\u002_module_activation_controller_v0_1.py

Default Gate evidence:

  .\U-UDP002\05_TESTS\02_ACCEPTANCE\U21C_GATE_20260818\03_GATE_EVIDENCE\U21C_ACCEPTANCE_EVIDENCE.json

Runtime dependencies:

  .\U-UDP002\01_PROGRAMS\u002_module_registry_v0_1.py
  .\U-UDP002\04_TEMP\u002_core_contract_schema_bundle_v0_1.json.txt
  .\U-UDP002\04_TEMP\u002_core_contract_validator_v0_1.py.txt
  .\U-UDP002\05_TESTS\01_FIXTURES\U20A_v0_1\valid_module_manifest.json

If the provided Gate evidence shows that the accepted run used an R2 output
directory, substitute the actual accepted directory in the generated CMD. Do
not search for or silently choose among multiple Gate evidence files.


6. REQUIRED PROMOTION SEQUENCE

The generated CMD must:

1. verify every required file exists;
2. use Python to parse the Gate evidence as UTF-8 JSON;
3. verify GateStatus and exact ReturnCapsule;
4. verify the Gate-recorded controller SHA equals the embedded accepted SHA;
5. compute the source candidate SHA-256 and require the same value;
6. remove read-only status from an existing destination only for controlled
   replacement of this exact destination path;
7. copy the source bytes to the destination;
8. use fc /B to prove source and destination byte identity;
9. compute destination SHA-256 and require the accepted value;
10. set the destination read-only;
11. run the promoted destination's complete U21C internal self-test with the
    accepted Registry, Schema Bundle, Validator and fixture;
12. require exact stdout line:

      U21C1 N-T24-A1-F4-B2-H5-E1-X0 C-0

13. leave stdout/stderr logs in U-UDP002/04_TEMP with new U21C promotion names;
14. print the final promotion capsule only after every check passes.

Do not regenerate code, normalize line endings, open files in text write mode
or copy through a mechanism that can alter bytes.


7. FINAL CAPSULE

Success:

  U21CP1 N-H1-C1-R1-X0 C-0

Meanings:

  H1 = Gate, source and destination identities agree
  C1 = byte-identical promotion copy completed
  R1 = relocated controller full internal self-test passed
  X0 = no unexpected promotion failure

Stable failures:

  U21CP1-ERROR-MISSING-INPUT C-7
  U21CP1-ERROR-GATE-EVIDENCE C-7
  U21CP1-ERROR-CONTROLLER-IDENTITY C-7
  U21CP1-ERROR-PROMOTION-COPY C-7
  U21CP1-ERROR-PROMOTED-SELFTEST C-7


8. USER EXECUTION

After downloading the generated CMD into U-UDP002/04_TEMP, run from C-CODE
root in Windows CMD:

  .\U-UDP002\04_TEMP\UDP002_U21C_PROMOTE_ACCEPTED_CONTROLLER_v0_1.cmd.txt


9. RETURN TO PROJECT LEAD

Return only:

  U21CP1 N-H1-C1-R1-X0 C-0

On failure return the complete first U21CP1-ERROR line and at most one short
Memo. Do not transcribe hashes, source code or long logs.


10. COMPLETION BOUNDARY

Promotion C-0 closes and freezes U21C as software infrastructure. It still does
not activate a real Camera/Radar Module. The next node will connect the frozen
Registry and Activation evidence into controlled pipeline resolution.


END
