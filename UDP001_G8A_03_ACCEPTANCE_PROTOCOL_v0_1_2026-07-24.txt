UDP001 — GATE 8A ACCEPTANCE PROTOCOL v0.1
Date: 2026-07-24

======================================================================
1. RETURN FORMAT
======================================================================

UDP001G8AR1
B1-abcd
B2-abcd
B3-abcd
B4-abcd
N-Gg-Pp-Uu-Rr-Ff-Xx
S1-Cc-Hh S2-Cc-Hh S3-Cc-Hh S4-Cc-Hh
S5-Cc-Hh S6-Cc-Hh S7-Cc-Hh
C-n

For C-0, return no Memo.

For C-1 through C-9, add at most one short Memo.

======================================================================
2. B CODES
======================================================================

B1 — orchestrator implementation

Positions:

1. orchestrator self-test 16/16
2. one complete import-safe Python file
3. uses sys.executable/list arguments/shell=False
4. accepted modules remain separate and unchanged

B2 — replay config and preflight

Positions:

1. seven modules and four controls resolve exactly once
2. source and two DBC inputs readable and hashed
3. control mappings/policies match accepted smoke
4. all seven module self-tests pass

B3 — ordered orchestration

Positions:

1. fixed Gate 1→2→3→4→5→6 profile→6 apply→7 sequence
2. stop-on-first-failure and capsule parsing work
3. expected smoke counts/policies hold
4. Gate 7 creates exactly six final files

B4 — safety and evidence

Positions:

1. no input/module/control mutation
2. no overwrite or partial success
3. orchestration manifest complete and final package path-safe
4. deterministic synthetic replay and config fingerprint pass

PASS requires every B position = 2.

======================================================================
3. N AND S LINES
======================================================================

N-Gg-Pp-Uu-Rr-Ff-Xx

- G completed data Gates
- P passed module self-tests
- U final decoded Update Stream rows
- R final Frame View rows
- F final package file count
- X blocking orchestration errors

S1–S7:

- C stage capsule/result code
- H protected module/control/input pre/post hash equality

Current replay PASS:

N-G7-P7-U599970-R20617-F6-X0
S1-C0-H1 S2-C0-H1 S3-C0-H1 S4-C0-H1
S5-C0-H1 S6-C0-H1 S7-C0-H1

======================================================================
4. RESULT CODES
======================================================================

C-0 = PASS
C-1 = generated source incomplete or import/syntax failure
C-2 = orchestrator self-test failure
C-3 = module/control/input discovery or config failure
C-4 = accepted-module self-test or protected-hash failure
C-5 = Gate 1–Gate 3 execution/capsule failure
C-6 = Gate 4–Gate 6 execution/capsule/policy failure
C-7 = Gate 7 config/package failure
C-8 = stage order, stop-on-failure, overwrite, or manifest failure
C-9 = other blocking failure; add one short Memo

======================================================================
5. ACCEPTANCE MEANING
======================================================================

C-0 proves one controlled program can replay the accepted staged U001 flow and
produce a new six-file candidate package without changing the accepted module
boundaries.

C-0 does not independently prove the candidate package equals the accepted
reference package.

After G8A C-0:

- freeze the candidate run;
- do not delete the accepted Gate 7 reference package;
- proceed to Gate 8B independent candidate-versus-reference validation.
