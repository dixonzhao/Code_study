UDP001 G10C2F1 - GUI v0.4a MISSING AUTO_DISCOVERY CORRECTION
Date: 2026-07-29
Status: CORRECTION AUTHORIZED; REAL RUN FORBIDDEN UNTIL CG2 PASS

======================================================================
1. CONFIRMED FAILURE AND OWNERSHIP
======================================================================

The current u001_data_preparation_workbench_gui_v0_4.py inherited the v0.3
interface but did not implement the required visible AUTO_DISCOVERY page,
Execute New Run action, or five-stage automatic Controller sequence.

The previous G10C2CG1 gate passed because it verified the pure command-plan
API and Controller boundary only.  It did not prove that those capabilities
were connected to visible GUI widgets.  Do not reinterpret the previous PASS
as product acceptance.

Do not run G10C2R1 with the incomplete v0.4 file.

======================================================================
2. COPILOT INPUT AND OUTPUT
======================================================================

Open a fresh Company Copilot chat and attach exactly these three files:

1. u001_data_preparation_workbench_gui_v0_4.py
2. u001_discovery_controller_v0_2.py
3. this correction specification

Read all three completely.  Modify the GUI, not the Controller.

Return one actual downloadable file named exactly:

u001_data_preparation_workbench_gui_v0_4a.py.txt

Do not return only a code block.  Do not add `download`, `下载`, spaces,
parentheses or other filename decoration.

After download, remove only the final `.txt` and save initially as:

C-CODE\UDP001\04_TEMP\u001_data_preparation_workbench_gui_v0_4a.py

Keep v0.3, incomplete v0.4 and Controller v0.2 unchanged.

======================================================================
3. REQUIRED REAL USER INTERFACE
======================================================================

The running GUI must visibly provide three separate tabs/pages:

1. AUTO_DISCOVERY (Recommended)
2. Controlled Profile (Legacy)
3. New Case / Workspace

The AUTO_DISCOVERY page must be an actual constructed Tkinter/ttk page.  A
constant, manifest, hidden method, comment or self-test description is not an
implementation.

Required visible editable inputs on AUTO_DISCOVERY:

- UDP001 Root
- Case Root
- Controller Path
- Run ID
- Source ID
- Segment ID
- Maximum Source Rows
- Chunk Size
- Batch Size
- Maximum Assignment Age Rows
- Maximum RSS MiB
- Update Buffer Rows
- Frame Buffer Rows
- Memory Check Interval Rows
- Minimum Free GiB

Required defaults:

Source ID = SOURCE_001
Segment ID = FULL_SOURCE
Maximum Source Rows = 10000
Chunk Size = 10000
Batch Size = 32
Maximum Assignment Age Rows = 200
Maximum RSS MiB = 2048
Update Buffer Rows = 256
Frame Buffer Rows = 32
Memory Check Interval Rows = 1000
Minimum Free GiB = 25

The fixed Frame key must be visibly displayed read-only:

Channel 2 / CAN ID 0x620 / Signal FrameCount

Required visible derived paths:

- Request Path
- Preflight Path
- Response Root
- Incomplete Root

Required visible buttons with these action meanings:

- Controller Self-test
- Create Request
- Preflight
- Run Request
- Validate Response
- Execute New Run
- Validate Existing Response
- Cancel
- Open Response Folder

The page must also contain a current-action/Run-ID status display, log area
showing complete Controller stdout/stderr, and an indeterminate progress
indicator for long work.

======================================================================
4. REQUIRED WIDGET REGISTRY
======================================================================

When the AUTO_DISCOVERY page is constructed, register the actual widgets in:

self.auto_discovery_widgets

This must be a dictionary containing these exact keys:

page
udp001_root
case_root
controller_path
run_id
source_id
segment_id
max_source_rows
chunksize
batch_size
max_assignment_age_rows
max_rss_mb
update_buffer_rows
frame_buffer_rows
memory_check_every_rows
minimum_free_gib
frame_key_display
request_path_display
preflight_path_display
response_root_display
incomplete_root_display
controller_selftest_button
create_request_button
preflight_button
run_request_button
validate_response_button
execute_new_run_button
validate_existing_response_button
cancel_button
open_response_folder_button
status_display
log_widget
progress_widget

Each value must be the actual constructed widget or associated Tk variable;
do not populate this registry with placeholder strings or None.

======================================================================
5. EXECUTE NEW RUN STATE MACHINE
======================================================================

Execute New Run must be connected to a real callback.  Its action order is
exactly:

controller_selftest
make_request
preflight
run_request
validate_response

Expose this import-safe pure function:

get_execute_new_run_action_order() -> tuple[str, ...]

It returns exactly the five action names above.

The actual Execute New Run callback must call or iterate this function; it may
not maintain a different hard-coded order.

Before starting, reject execution if any of these already exists:

- request path;
- preflight path;
- response root;
- incomplete root.

Do not delete or overwrite an existing path and do not silently alter Run ID.

Each stage launches only Controller v0.2 through a list-of-strings command,
sys.executable and shell=False.  The GUI must never directly launch G1, G10A,
G10B or any Controller child.

Proceed to the next stage only after exit code 0 plus its exact accepted token:

U001-G10C1R-SELFTEST-PASS-40
G10C1-REQUEST-CREATED C-0
G10C1-PREFLIGHT-PASS C-0
G10C1-RUN-PASS C-0
G10C1-VALIDATE-PASS C-0

Any error stops the sequence, unlocks safe controls and preserves evidence.
After final validation, display exactly:

Ready for U002

======================================================================
6. THREADING AND PRESERVED FEATURES
======================================================================

Controller subprocesses must run outside the Tk main thread using the accepted
queue/after architecture or an equivalent thread-safe implementation.  Only
one Controller process may be active.  Cancel must terminate the active
process tree without deleting incomplete evidence.

Preserve working v0.3/v0.4 behavior:

- New Case workspace creation and read-only source copying;
- Case discovery and selection;
- Controlled Profile legacy G8A2 path;
- no-overwrite behavior;
- output-folder opening;
- existing GUI logging/cancellation behavior;
- build_auto_discovery_command_plan API accepted by CG1.

Do not move data semantics, decoding or discovery logic into the GUI.

======================================================================
7. REQUIRED IMPORT-SAFE UI DECLARATION
======================================================================

Expose these constants/functions for independent verification:

PROGRAM_VERSION = "0.4a"
DISCOVERY_CONTROLLER_NAME = "u001_discovery_controller_v0_2.py"
SELFTEST_TOKEN = "U001-GUIV04A-SELFTEST-PASS-40"

get_auto_discovery_ui_contract() -> dict

The returned dictionary has exactly these top-level keys:

PageTitle
Fields
Actions
Sequence
Defaults
FrameKey
WidgetRegistryKeys

Its values must match sections 3-5.  The actual page-construction method must
reference get_auto_discovery_ui_contract(), and the Execute New Run callback
must reference get_execute_new_run_action_order().

======================================================================
8. SELF-TEST AND HUMAN VISIBLE CHECK
======================================================================

Retain previous deterministic tests and add tests for:

- exact UI contract;
- all required field names/defaults;
- all nine action names;
- exact five-stage sequence;
- actual page-construction source references UI contract;
- actual Execute callback source references sequence function;
- actual widget-registry key coverage;
- Controller-only command boundary;
- exact success-token chain;
- failure stops later stages.

Required final self-test line:

U001-GUIV04A-SELFTEST-PASS-40

A larger count is acceptable.

After generation, do not open a real Case.  From C-CODE CMD run:

python .\UDP001\04_TEMP\u001_data_preparation_workbench_gui_v0_4a.py --self-test

Require the exact token above, then run the separate G10C2CG2 independent
gate supplied by Codex.  Only after CG2 passes may the GUI be opened for a
visible-layout smoke check.

END OF G10C2F1 CORRECTION SPECIFICATION
