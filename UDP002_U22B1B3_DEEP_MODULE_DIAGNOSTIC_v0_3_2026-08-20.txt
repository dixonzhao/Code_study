UDP002 U22B1B3 Module深层诊断说明
版本：v0.3
日期：2026-08-20

一、当前状态

v0.2结果：

U22B1B3 N-F0-V0-S0-Q0-P0-X1 C-7 R-RUNNER K-MODULE_EXIT J1

这证明：

1. Preflight已通过；
2. Run已经进入Executor；
3. S0或S1中的一个Module以非零Exit Code退出；
4. 原失败Run已被保留，不能直接删除或改回PREPARED。

v0.3会自动沿以下证据链继续下钻：

run_status.json
→ 失败Module
→ 01_LOGS中的Execution Record
→ DiagnosticRoot
→ module_result.json
→ stdout.txt / stderr.txt
→ 最内层C-7 Reason或Python异常

二、下载文件

将以下三个文件直接下载到：

.\U-UDP002\04_TEMP

1. u002_u22b1b3_real_s1_reconstruction_run_v0_3.py.txt
2. udp002_u22b1b3_real_s1_reconstruction_acceptance_v0_3.py.txt
3. 本说明文件

不要删除原失败Run，不要修改run_status.json，不要让Copilot重写程序。

三、执行命令

从C-CODE根目录使用Windows CMD运行：

python .\U-UDP002\04_TEMP\udp002_u22b1b3_real_s1_reconstruction_acceptance_v0_3.py.txt --accept --workspace-root .\U-UDP002

四、返回规则

只回传程序输出的最后一行：

U22B1B3 ... C-7 R-RUNNER K-最内层原因 J1

例如：

K-VALUEERROR_MISSING_CAMERA_SOURCE_COLUMN
K-KEYERROR_SIGNAL_ROLE
K-MODULE_EXIT_CODE_7

完整证据会自动保存到：

.\U-UDP002\04_TEMP\u22b1b3_run_diagnostic_v0_3.json
.\U-UDP002\04_TEMP\u22b1b3_acceptance_diagnostic_v0_3.json

无需人工打开或抄写JSON。

五、后续规则

取得最内层原因后：

1. 保留本次FAILED Run作为失败证据；
2. 只修复拥有该问题的Module或接口层；
3. 生成新的受控Retry Run身份；
4. 不原地篡改失败Run；
5. 再执行完整技术验收。
