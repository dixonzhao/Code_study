UDP001 FINAL-BUILD — ZIP EMPTY-DIRECTORY REPAIR
Date: 2026-07-30
Status: EXISTING RELEASE ONLY; REBUILD FORBIDDEN

Observed acceptance result:

UDP001-FINAL-FAIL-ZIP-CASES-DIRECTORY C-7

The published folder contains the required empty UDP001\03_CASES directory,
but the ZIP archive omitted its empty directory entry. If the ZIP is extracted,
03_CASES would therefore disappear. This is a real packaging defect limited to
the ZIP container; the runtime and package folder remain accepted.

Download u001_final_zip_empty_directory_repair_v0_1.py.txt, remove only final
.txt and place it in UDP001\04_TEMP. From C-CODE Windows CMD run:

python .\UDP001\04_TEMP\u001_final_zip_empty_directory_repair_v0_1.py --release-root .\UDP001\04_TEMP\U001_FINAL_RELEASE

The repair program:

1. requires the folder-side 03_CASES directory to exist and be empty;
2. verifies the ZIP is readable and the directory entry is absent;
3. calculates the original ZIP SHA-256;
4. refuses repair if the Manifest references that ZIP hash;
5. creates an external backup beside the release;
6. streams all original ZIP entries into a temporary ZIP;
7. adds only U001_Portable_v0_2/UDP001/03_CASES/;
8. validates original file CRC/size preservation;
9. atomically replaces the ZIP and writes a local repair report.

Expected:

UDP001-FINAL-ZIP-REPAIR N-F1-E1-M0-B1-R1-V1-X0 C-0

After repair C-0, rerun the existing v0.2 final acceptance. Then run the
no-argument GUI test. Return only final acceptance and GUI success lines.

