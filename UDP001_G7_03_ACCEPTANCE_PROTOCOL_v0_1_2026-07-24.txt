UDP001 — GATE 7 ACCEPTANCE PROTOCOL v0.1
Date: 2026-07-24

======================================================================
1. RETURN FORMAT
======================================================================

UDP001G7R1
B1-abcd
B2-abcd
B3-abcd
B4-abcd
N-Uu-Rr-Ss-Kk-Vv-Ww-Ff-Xx
C-n

No free-form memo is needed when C-0.

For C-1 through C-9, add at most one short Memo line.

======================================================================
2. B CODES
======================================================================

B1 — generated program and regression safety

1 = incomplete
2 = pass
3 = fail
4 = not tested

Positions:

1. self-test passed 16/16
2. Gate 1–Gate 6 modules remained unchanged
3. import is side-effect free
4. one complete Python source file

B2 — discovery/config/input integrity

Positions:

1. every required accepted input resolved exactly once
2. local config schema and provisional smoke identity valid
3. input hashes captured before build
4. Gate 4/Gate 6 schema, row, and FrameCount invariants valid

B3 — six outputs and cross-file agreement

Positions:

1. update stream copied byte-for-byte with equal SHA-256
2. Frame View copied byte-for-byte with equal SHA-256
3. Signal Catalog/Quality Report/Manifest schemas valid
4. Review View equals configured Frame View columns row by row

B4 — package safety and determinism

Positions:

1. exactly six files inside final package
2. manifest absolute-path/record-value leak count is zero
3. no input or existing output was overwritten
4. deterministic repeat/config-fingerprint checks passed

Gate 7 PASS requires every B position = 2.

======================================================================
3. N LINE
======================================================================

N-Uu-Rr-Ss-Kk-Vv-Ww-Ff-Xx

- U decoded update rows
- R Frame View rows
- S Signal Catalog rows
- K signals included in Frame View
- V Review View rows
- W Review View columns including FrameCount
- F final package file count
- X blocking package errors

Current smoke PASS:

N-U599970-R20617-S6-K5-V20617-W6-F6-X0

======================================================================
4. RESULT CODES
======================================================================

C-0 = PASS
C-1 = generated source incomplete or syntax/import failure
C-2 = self-test failure
C-3 = missing/duplicate input discovery failure
C-4 = config/catalog/policy mapping failure
C-5 = accepted update-stream invariant/hash failure
C-6 = accepted Frame View invariant/hash failure
C-7 = Quality/Manifest/Review View cross-file failure
C-8 = exact-six-file, overwrite, path-leak, or deterministic-build failure
C-9 = other blocking failure; add one short Memo

======================================================================
5. ACCEPTANCE MEANING
======================================================================

C-0 completes the staged U001 core modules and proves they can produce one
contract-compliant six-file smoke package from the accepted Gate outputs.

C-0 does not yet mean:

- legacy P001–P003 are retired;
- a final GUI exists;
- a production orchestrator exists;
- a full Raw CAN Golden Run has been executed through one command;
- camera target fusion or U002 analysis is approved.

After Gate 7 acceptance, the next controlled node is:

UDP001 Gate 8 — U001 orchestration and independent end-to-end validator.

Gate 8 will call accepted modules in sequence and compare the new pipeline
against frozen legacy/reference outputs before replacement approval.
