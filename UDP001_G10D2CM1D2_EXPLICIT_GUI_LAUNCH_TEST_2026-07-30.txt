UDP001 G10D2C-M1D2 — EXPLICIT GUI LAUNCH TEST
Date: 2026-07-30

M1D1 proved that U001_Workbench.exe starts but exits with argparse return code
2 when launched without arguments. The accepted executable advertises the
explicit --run-gui action.

From C-CODE Windows CMD run:

.\UDP001\04_TEMP\G10D2C_RELEASE_01\U001_Portable_v0_2\UDP001\00_LAUNCHER\U001_Workbench.exe --run-gui

If the GUI opens, inspect that the current v0.4a controls are visible, then
close normally and return:

G10D2CM1D2-GUI-VISIBLE-NORMAL-CLOSE C-0

If no GUI appears, return the visible error line or:

G10D2CM1D2-NO-GUI C-7

Do not rebuild yet. A C-0 proves the GUI payload is healthy and isolates the
remaining defect to no-argument entrypoint dispatch. The final correction will
make double-click/no-argument launch default to GUI while preserving all CLI
self-test and hosted-test actions.

