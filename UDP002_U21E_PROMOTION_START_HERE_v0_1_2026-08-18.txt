UDP002 U21E ACCEPTED BINDER PROMOTION

Task ID: U21E-PROMOTION
Version: 0.1
Date: 2026-08-18
Owner: Zhao Zhili
Status: APPROVED FOR COMPANY-SIDE GENERATION AND EXECUTION


1. PURPOSE

Promote only the exact Run Preflight and U001 Data Binder accepted by the U21E
independent Gate, then rerun its complete self-test after relocation.

Accepted Gate capsule:

  U21EG1 N-R2-P4-F12-I6-X0 C-0


2. PROVIDE TO COPILOT

1. this promotion START_HERE document;
2. candidate u002_run_preflight_data_binder_v0_1.py.txt;
3. U21E_ACCEPTANCE_EVIDENCE.json from the passing formal Gate v0.2 run.

Read all inputs completely before generation. The diagnostic Gate output is not
promotion evidence.


3. GENERATE EXACTLY ONE FILE

Return the complete downloadable Windows CMD file:

  UDP002_U21E_PROMOTE_ACCEPTED_BINDER_v0_1.cmd.txt

Initial location:

  U-UDP002/04_TEMP

Do not return only prose, status text or partial code.


4. ACCEPTED IDENTITY

Read the actual passing Gate evidence and locate the exact SHA-256 recorded for
the tested candidate binder. Embed it as EXPECTED_BINDER_SHA256 in the CMD. Do
not require the user to type or copy a SHA-256.

Require from actual evidence:

  GateStatus = PASS
  ReturnCapsule = U21EG1 N-R2-P4-F12-I6-X0 C-0

Use the real evidence field paths. Do not invent a replacement schema and do
not use the failed v0.1 Gate or diagnostic evidence.


5. FIXED PATHS FROM C-CODE ROOT

Source:

  .\U-UDP002\04_TEMP\u002_run_preflight_data_binder_v0_1.py.txt

Destination:

  .\U-UDP002\01_PROGRAMS\u002_run_preflight_data_binder_v0_1.py

Passing Gate evidence:

  Use the exact retained path to the v0.2 passing run's
  04_GATE_EVIDENCE\U21E_ACCEPTANCE_EVIDENCE.json.

If multiple Gate directories exist, require an explicit exact path or bind the
one recorded by the formal execution. Never select by newest modification time.


6. REQUIRED SEQUENCE

The generated CMD must:

1. verify source, destination parent and passing Gate evidence;
2. parse evidence as UTF-8-sig JSON;
3. verify PASS and exact ReturnCapsule;
4. extract and validate the Gate-recorded binder SHA-256;
5. compute source SHA-256 and require exact agreement;
6. refuse to overwrite a non-identical existing destination;
7. copy source bytes to destination without line-ending normalization;
8. prove source/destination byte identity with `fc /B` and SHA-256;
9. set the destination read-only;
10. run the relocated destination with `--self-test`;
11. require exact stdout:

      U21E1 N-T24-P3-U6-B8-C4-S2-X0 C-0

12. retain bounded stdout/stderr and promotion evidence under 04_TEMP;
13. print success only after every check passes.

No Binder code change, text rewrite or newline conversion is permitted.


7. FINAL CAPSULE

Success:

  U21EP1 N-H1-C1-R1-X0 C-0

Meanings:

  H1 = Gate evidence, source and destination identities agree
  C1 = byte-identical promotion completed
  R1 = relocated binder full self-test passed
  X0 = no unexpected failure

Stable failure:

  U21EP1-ERROR-<STABLE_REASON> C-7


8. EXECUTION

After download into U-UDP002/04_TEMP, run from C-CODE root in Windows CMD:

  .\U-UDP002\04_TEMP\UDP002_U21E_PROMOTE_ACCEPTED_BINDER_v0_1.cmd.txt


9. RETURN

Return only:

  U21EP1 N-H1-C1-R1-X0 C-0

On failure return the first complete ERROR line and at most one short Memo.


10. COMPLETION BOUNDARY

Promotion C-0 freezes U21E and authorizes U21F Controlled Stage Executor. It
does not execute any production analysis Module.

END
