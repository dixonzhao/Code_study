UDP001 — GATE 8A EXECUTION COMMAND HANDOFF G8AE1
Date: 2026-07-24

======================================================================
1. WHEN TO USE
======================================================================

Use only after:

- u001_golden_replay_orchestrator_v0_1.py.txt is generated;
- it is saved as u001_golden_replay_orchestrator_v0_1.py;
- the same Raw CAN, CH1 DBC, CH2 DBC, accepted control files, and seven
  accepted module files remain available on the company computer.

======================================================================
2. EXACT MESSAGE TO COMPANY COPILOT
======================================================================

请读取UDP001_G8AE1_01_EXECUTION_COMMAND_HANDOFF_2026-07-24.txt。

不要修改或重新生成任何Python程序。

请根据当前公司电脑中真实存在的路径，生成三条可直接复制到VS Code
Terminal运行的单行命令：

1. SELFTEST
运行u001_golden_replay_orchestrator_v0_1.py --self-test

2. MAKECONFIG
运行--make-config，并提供以下真实绝对路径：

--module-root
--accepted-control-root
--source
--dbc-ch1
--dbc-ch2
--out（u001_g8a_replay_config.json）

3. RUN
运行--run：

--config使用上一步JSON；
--out-root使用一个全新的UDP001_G8A_REPLAY目录。

必须使用当前电脑真实Python启动方式。所有路径加双引号。不得输出
PATH_TO、...或其他占位符。不得自动执行。

路径明确时只输出：

UDP001-G8AE1-COMMANDS
SELFTEST-<完整单行命令>
MAKECONFIG-<完整单行命令>
RUN-<完整单行命令>

路径不明确时只输出：

UDP001-G8AE1-PATH-BLOCKED
UNKNOWN-<无法确定的参数名>

不要猜路径，不要增加解释。

======================================================================
3. USER EXECUTION
======================================================================

Run:

1. SELFTEST
2. MAKECONFIG
3. RUN

Stop if:

- self-test is not UDP001-G8A-SELFTEST-PASS-16;
- config discovery reports zero or duplicate match;
- any Stage stops;
- final C code is not C-0.

On success, return to ChatGPT only the complete content of:

udp001_g8a_return_capsule.txt
