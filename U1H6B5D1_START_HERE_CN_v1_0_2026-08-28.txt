U001 H6B5D1：Frame分配与无效Update只读诊断
===========================================

为什么暂停H4
------------
H6B5程序执行成功，但185,495个Update中只有6,653个有效，且52,389个Source Row没有分配到Frame。
本诊断只读取H6B5结果，区分：
- FrameCount覆盖范围不足；
- 相邻FrameCount的Source Row间隔超过200；
- Frame assignment导致的无效；
- 三条Exact invalid规则导致的内在无效；
- 数据类型或Enum问题。

本程序不会修改B3、B4或B5结果，也不会进入H4。

放置位置
--------
将两个文件直接放入：
C-CODE\U-UDP001\04_TEMP

从 C-CODE 目录依次执行（仅三行）
---------------------------------
python U-UDP001\04_TEMP\u001_h6b5d1_frame_validity_diagnostic_v1_0.py.txt --prepare
python U-UDP001\04_TEMP\u001_h6b5d1_frame_validity_diagnostic_v1_0.py.txt --self-test
python U-UDP001\04_TEMP\u001_h6b5d1_frame_validity_diagnostic_v1_0.py.txt --execute

需要回传
--------
回传execute最后两行：

H6B5D1-...
H6B5D2-...

不需要打开CSV或JSON，也不需要手工统计。
