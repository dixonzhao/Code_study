UDP002 U21F2B LINE-101 STATIC CALL-CHAIN PROBE D4

Task ID: U21F2B-D4
Version: 0.1
Date: 2026-08-19
Owner: Zhao Zhili
Status: READ-ONLY STATIC DIAGNOSIS


1. PURPOSE

Determine what the frozen Executor's selftest line 101 actually evaluates
inside the outer `ck(...)` call, including nested calls, literal test label,
reachable subprocess operations, command modes and timeout values. Do not run,
import, edit or copy the Executor.


2. REQUIRED WORKING SET

Give a fresh Copilot chat exactly two files:

1. this instruction;
2. `u002_controlled_stage_executor_v0_2.py.txt`.

At execution time the frozen Executor must be directly at:

  U-UDP002\04_TEMP\u002_controlled_stage_executor_v0_2.py.txt

No diagnostic logs or other project dependencies are required.


3. GENERATE EXACTLY ONE FILE

  u002_u21f2b_line101_static_probe_v0_1.py.txt

Use Python standard library only and put it directly in U-UDP002\04_TEMP. Do
not generate BAT, PowerShell, corrected Executor, report or replacement source.


4. READ-ONLY STATIC ANALYSIS

The probe must:

- require `--executor PATH`;
- reject symlinks and non-regular files;
- hash the Executor before and after and require equality;
- read with UTF-8-sig support and parse with ast.parse();
- never compile, import, run or rewrite the Executor;
- never print source lines, hashes or absolute paths.

Locate the narrowest statement whose source range contains line 101. Require it
to be inside function `selftest` and contain outer direct call `ck`.


5. REQUIRED FACTS

From that statement, extract in lexical/evaluation order:

- statement kind and ordinal among executable selftest statements;
- total top-level executable statements in selftest;
- string constants used as test/check labels;
- every nested Call target, not only the outer ck;
- comprehensions, generator expressions and loop context;
- locally defined functions directly reachable from nested calls, recursively
  to depth four with cycle protection;
- subprocess.run/Popen/check_call/check_output/communicate/wait usage;
- literal or statically named timeout values and their defining assignment;
- command-mode literals among `--self-test`, `--module-worker`,
  `--dependency-preflight` or NONE;
- whether reachable code can invoke the Executor's full self-test again;
- whether Python eager argument evaluation means a nested call is running
  before ck() itself can return.

Use UNKNOWN rather than inference when a value is not statically resolvable.


6. SELF-TEST

`--self-test` uses private synthetic Python text and proves exactly 14 checks:
line-range selection, outer/nested call ordering, label extraction, eager
argument identification, local call graph, depth/cycle control, subprocess
recognition, four command modes, literal/named timeout resolution, loop context,
statement ordinal, identity preservation and X0 uncontrolled failures.

Success:

  U21F2BD4ST N-T14-A5-G4-I3-X0 C-0


7. CMD FROM C-CODE ROOT

Self-test:

  python ".\U-UDP002\04_TEMP\u002_u21f2b_line101_static_probe_v0_1.py.txt" --self-test

If C-0, static probe:

  python ".\U-UDP002\04_TEMP\u002_u21f2b_line101_static_probe_v0_1.py.txt" --probe --executor ".\U-UDP002\04_TEMP\u002_controlled_stage_executor_v0_2.py.txt" --line 101


8. RETURN

  U21F2BD4 N-L101-S<s>-Q<q>-C<c>-G<g>-T<t>-M<m>-E<e>-I1-X0 C-0

Meanings:

- S statement ordinal;
- Q total top-level selftest statements;
- C nested call count on the selected statement;
- G reachable local-function count;
- T statically resolved timeout value count;
- M command-mode count;
- E 1 when eager nested argument evaluation is confirmed, else 0/9.

Then at most three short lines:

  MEMO1-LABEL-<literal-or-NONE>
  MEMO2-CALLCHAIN-<call1>><call2>><call3>... (maximum 120 characters)
  MEMO3-TIMEOUT-<name=value-or-UNKNOWN>-MODE-<mode>-RECURSE-<0|1|9>

Failure:

  U21F2BD4 N-L101-S0-Q0-C0-G0-T0-M0-E9-I<i>-X<x> C-7 K-<REASON>


9. BOUNDARY

D4 C-0 supplies static facts only. It does not accept U21F2B or authorize a
source change. The project lead will decide whether the observed duration is an
expected test budget or one localized implementation defect.

END OF D4
