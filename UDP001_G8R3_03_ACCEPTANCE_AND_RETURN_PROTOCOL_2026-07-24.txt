UDP001 G8R3 — ACCEPTANCE AND RETURN PROTOCOL

======================================================================
1. REQUIRED SELF-TESTS
======================================================================

All must pass before real execution:

UDP001-G7R3-SELFTEST-PASS-20
UDP001-G8AR3-SELFTEST-PASS-20
UDP001-G8BR3-SELFTEST-PASS-20

======================================================================
2. CORRECTED REFERENCE ACCEPTANCE
======================================================================

Pass only when:

- Gate 7 v0.2 source used;
- exactly six final files;
- SourceId SOURCE_001;
- IdentityStatus PROVISIONAL_SMOKE;
- matching provisional warning exactly once;
- identity consistency 1;
- Update required-column-missing count 0;
- ClockDomain bad count 0;
- NativeTimestamp nonblank count 0;
- MessageCounter nonblank count 0;
- Update row count 599970;
- Frame row count 20617;
- package inputs unchanged.

======================================================================
3. CORRECTED CANDIDATE ACCEPTANCE
======================================================================

Pass only when:

- Gate 8A v0.2 source used;
- Gate 7 v0.2 selected;
- all seven stage C codes 0;
- all seven protected-hash flags 1;
- exactly six final files;
- SourceId SOURCE_001;
- IdentityStatus CONTROLLED_REPLAY;
- no provisional identity warning;
- candidate identity consistency 1;
- Update required-column-missing count 0;
- Update row count 599970;
- Frame row count 20617;
- replay inputs/modules/control files unchanged.

======================================================================
4. INDEPENDENT VALIDATION ACCEPTANCE
======================================================================

Pass target:

N-R6-C6-E3-S2-M1-Q6-I2-U599970-F20617-X0
D1-E1 D2-E1 D3-S1 D4-S1 D5-M1 D6-E1
Q-R6-S6-O0
I-R1-C1
C-0

Interpretation:

- D1/D2/D6 exact;
- D3/D4 semantic equal;
- D5 normalized equal under narrow approved runtime rules;
- six intermediate quality JSON pairs semantic equal;
- zero unapproved quality difference;
- both final identities internally consistent;
- no input mutation.

======================================================================
5. FAILURE ROUTING
======================================================================

If self-test fails:

- stop before real data;
- fix only the failing corrected program;
- keep the same G8R3 specification.

If reference or candidate build fails:

- do not edit its package;
- preserve failure folder;
- return the failing stage capsule plus one short MEMO.

If Gate 8B v0.2 fails:

- do not change the validator whitelist;
- return its capsule and at most three MEMOs;
- route by first failing D/Q/I code.

======================================================================
6. LOW-BANDWIDTH RETURN
======================================================================

Return only the complete text from:

udp001_g8r3_return_capsule.txt

Expected compact form:

UDP001G8R3R1 B1-.... B2-.... B3-.... B4-....
N-R6-C6-E3-S2-M1-Q6-I2-U599970-F20617-X0
D1-E1 D2-E1 D3-S1 D4-S1 D5-M1 D6-E1
Q-R6-S6-O0 I-R1-C1 C-0

Use hyphen, not equals sign. Groups may be placed four per line for manual
checking.

Free text is used only on failure:

MEMO1-...
MEMO2-...
MEMO3-...

======================================================================
7. DECISION RULE
======================================================================

C-0:

Gate 8 reproducibility smoke is accepted for the 100000-source-row controlled
dataset. Proceed to the full-data U001 execution plan. This does not yet prove
all future signals, DBCs, segments, or logs.

C nonzero:

Gate 8 remains open. Do not proceed by weakening validation.
